# ドメイン発見: 未解決の論点・残る検討事項（改訂版）

> 本ドキュメントは改訂スコープ（2026-04-30）における未確定事項のリストである。
> 初版で挙げていた論点・質問は本改訂で大半が解決済み。下記 A 節に解決済みとして記録、B 節以降に残る論点を整理する。
> シナリオ B 反映版（2026-05-01）: オンボーディングのウォークスルー結果を A 節に統合、B 節に残った論点を新規 OQ として追記。
> シナリオ C 反映版（2026-05-01）: 未分類修正と memorization 学習のウォークスルー結果を A.3 節に統合、OQ-3/4/5/8/11/17/18 を更新、新規 OQ を B 節に追記。
> 初版は git 履歴で参照可能。

## A. 解決済み（改訂で確定）

| ID | 論点 | 決定 |
| --- | --- | --- |
| 論点1 | 「決済」と「資金移動」の分離 | **分離する**（別概念として扱う） |
| 論点2 | クレカ会計方式 | **発生主義**（利用時点で支出+未払金計上、引落で消込） |
| 論点3 | 「夫婦の共有」と「個人」の区別 | **取引ごとに費用区分フラグ**（世帯／個人(夫)／個人(妻)／経費(会社)、自動推定+手動補正）+ **口座/カードに所有者属性**（夫／妻／共有）。経費(会社) は家計分析対象外 |
| 論点4 | 外部連携の方針 | **主データソース: メール通知（Gmail API）**。**補助データソース: カード／銀行明細CSV（手動アップロード、月1）**。LINE 公式アカウントで CSV 取込忘れリマインダーを夫婦共通トークルームに配信。アグリゲーター連携・各社API・スクレイピングは対象外 |
| Q-A | 現状の家計管理方法 | スコープ確定により実質不要 |
| Q-B | 費目（カテゴリ）の粒度 | **大分類4つ**（住居・光熱・通信／食費／娯楽／その他）。後から細分化・統合可 |
| Q-C | 目標は1つか複数か | **目標機能はスコープ外** |
| Q-D | 相手に見られたくない支出 | プライバシー3段階。**世帯**=フルオープン／**個人(夫/妻)**=明細は本人のみ・合計は相手可視／**経費(会社)**=本人のみ可視（相手から完全に見えない） |

### A.2 シナリオ B（オンボーディング）ウォークスルーで解決した論点

| ID | 論点 | 決定 |
| --- | --- | --- |
| 論点5 | 役割（夫/妻）の判定方式 | **設定値（HUSBAND_LINE_USER_ID / WIFE_LINE_USER_ID）の許可リスト** で自動判定。第三者アクセスはエラー画面で拒否 |
| 論点6 | 初回 LINE Login 失敗時のリカバリ | **LIFF SDK 任せ**（独自リトライ UI なし、専用イベントなし）。家族用なので最小限 |
| 論点7 | Gmail OAuth の実装方式 | スコープ `gmail.readonly`、LIFF 内ブラウザは `disallowed_useragent` のため `liff.openWindow({external: true})` で **OS 標準ブラウザに切り出し**、トークンは Parameter Store（KMS）に保存 |
| 論点8 | Phase 2 セクションの順序強制度 | **A（Gmail OAuth）と B（初期残高）のみ必須・順序強制**、C/D/E は seed 投入で確認のみ任意、F は推奨だがスキップ可 |
| 論点9 | 初期残高の基準時刻 | **ユーザー入力時点**。後から修正可（運用中の残高補正と同枠組み） |
| 論点10 | 三井住友カード 未払金の初期値 | **ゼロから開始**、運用開始月以降のメール+CSV で積上げ。過去未払金は遡及取得しない |
| 論点11 | 残高のプライバシー | **本人のみ可視**。ただし **別銀行貯蓄 + NISA は合計を配偶者も可視** |
| 論点12 | カテゴリマスタの構造 | 規定 4 種は **世帯共有・削除改名不可**、追加カテゴリは **個人別**（追加・改名・削除可） |
| 論点13 | 経費種別マスタの構造 | カテゴリと同構造。規定 5 種は **世帯共有・削除改名不可**、追加項目は **個人別** |
| 論点14 | 月次上限の seed 値投入 | DB 初期化時に **個人別** に seed 投入（夫AI 10,000 / 妻AI 3,000 等）。Phase 1 の役割で自動紐付け |
| 論点15 | 「無制限」の DB 表現 | **`is_unlimited` 等の専用カラム**（マジックナンバー -1 等は使わない） |
| 論点16 | 取込開始タイミング | 個別 Phase 2 (A+B) 完了で **翌日 0:00 から日次バッチ稼働**（即時メール取込なし）|
| 論点17 | 運用開始の発火条件 | 夫婦両方の Phase 2 (A+B) 完了で **自動発火 + ログ記録**（操作ボタンなし）|
| 論点18 | 配偶者完了待ち中の挙動 | 自分のダッシュボード・取引一覧は **使える**、月次レポート画面のみ「準備中」で封印 |
| 論点19 | 配偶者完了の検知方式 | **画面ロード時のみ判定**（ポーリング・WebSocket は使わない）。アプリ内バナーで通知 |
| 論点20 | 運用開始月の月次レポート | **「不完全月」フラグ付き** で生成。**翌月 5〜10 日の通常サイクルで CSV 再取込 → レポート生成・配信** |
| 論点21 | LINE Bot 通知の有効化タイミング | **Phase 4（運用開始）と同時**。共通トークルーム ID は **join Webhook 自動取得**、Phase 4 完了時にテスト送信 |
| 論点22 | 日次バッチの取込範囲 | **過去 5 日以前のメールを毎回スキャン**、Gmail message ID で重複除外（Phase 2-F とのギャップ吸収・冗長化保険）|
| 論点23 | LINE 通知単発失敗の扱い | **完全スキップ**（ログのみ）。連続失敗時のみフェイルセーフメール（OQ-14 と整合） |
| 論点24 | Phase 2-F（初期 CSV/PDF 取込） | 対象月 = **運用開始日の当月**、取込範囲 = **CSV/PDF 内の全取引**、推奨だがスキップ可、CSV 取込は即時実行 |

### A.3 シナリオ C（未分類修正と memorization 学習）ウォークスルーで解決した論点

| ID | 論点 | 決定 |
| --- | --- | --- |
| 論点25 | 用語: 「目的軸」の改称 | **「費用区分」に改称**。月次レポートの既存表記「世帯費用」「個人費用」「経費(会社)合計」と直結し、収入取引には適用されない概念であることを明示 |
| 論点26 | 学習データのスコープ | **完全個人別（F-1）**。夫婦の学習データは別テーブル。配偶者の学習は一切波及せず、規定4種カテゴリでも個別学習。プライバシーと実装単純性を優先したトレードオフ |
| 論点27 | 学習データの構造 | **フィールド独立（T-2）**。加盟店名 → カテゴリ／費用区分／経費種別 を別マッピングで保持。「同じ加盟店でカテゴリは固定だが費用区分は揺れる」ケースに対応 |
| 論点28 | 費用区分の学習スコープ | **4 値全て学習対象**（世帯／個人(夫)／個人(妻)／経費(会社)）。F-1 配下では個人(配偶者) 値は出現しないが、ルックアップ動作・上書き判定の一貫性のため記録する |
| 論点29 | Amazon 取引の学習方式 | **加盟店名「AMAZON.CO.JP」は学習対象外**、商品カテゴリをキーに学習する **別テーブル（X-1）**。突合タイムアウトは双方向 3 日 |
| 論点30 | 未分類の発見動線 | **ダッシュボードに「未分類: N件」ウィジェットを常設**（C-2）。LINE 通知への混載はしない（明細を載せない原則と整合） |
| 論点31 | 未分類取引の費用区分デフォルト | **カード所有者の個人(本人)**（A-1）。経費種別は NULL |
| 論点32 | 未分類の集計上の扱い | **独立カテゴリとして表示**（B-1）。修正動機を与えるため目立たせる |
| 論点33 | 修正画面の構造 | **1 画面で 3 軸（カテゴリ／費用区分／経費種別）を編集**、未分類軸のみ赤マーカー（D-3） |
| 論点34 | 修正画面からのマスタ追加 | **修正画面に「+ 新規追加」を出し、その場で新規カテゴリ／経費種別を作成可**（E-1）|
| 論点35 | 学習データの反映タイミング | **保存ボタンで即時反映**（I-1）|
| 論点36 | 過去未分類取引への遡及 | **確認ダイアログ**（J-3）。対象は **本人の未分類取引のみ**（既分類・配偶者取引は触らない）|
| 論点37 | 既存学習ルール上書き | **「以後このルールに従いますか？」確認ダイアログ**（L-4）で「今回限り」と「ルール上書き」を分岐 |
| 論点38 | 学習無効化機能 | 修正画面に **「この加盟店は学習しない」チェックボックス**（M-1）。チェック後は常に「未分類」で取り込まれる |
| 論点39 | 夫婦間の分類不整合 | **容認**（K-1）。F-1 を採った時点で構造的に許容する思想 |
| 論点40 | CSV 取込時の一括分類 | CSV 取込完了画面で **新規N件／自動分類N件／未分類N件 サマリ表示**、その場で一括分類画面へ誘導（N-1）。同一加盟店の複数取引は 1 ルールに集約 |
| 論点41 | 未分類のままレポート生成 | **許容**（O-1、シナリオA 既存決定の継承）。後日分類で過去レポートが書き換わるのは仕様 |
| 論点42 | カテゴリ／経費種別の削除フロー | 移動先カテゴリ（or 経費種別）+ 費用区分をユーザー選択（Q-2 改）／ 学習データを移動先にリマップ（R-2 改）／ 名称変更は副作用なし（学習は id 参照） |
| 論点43 | スナップショット概念の廃止 | **廃止**。ダッシュボード・月次レポート画面はリアルタイム集計に統一。配信時点の値の凍結は **LINE 配信ログ**（送信 payload を json 保持）で代替 |
| 論点44 | Amazon 突合タイムアウト | **3 日**（双方向）。タイムアウト後は「Amazon 注文不明」フラグで未分類確定、ユーザー手動分類待ち（V-2）|

### A.4 Phase 3（戦略的設計）で解決した論点

| ID | 論点 | 決定 |
|---|---|---|
| 論点45 | コンテキスト分割（並行して Phase 3 へ進む の項目） | 取引取込 / 自動分類・学習 / 家計分析 / 残高・資産推移管理 / 経費精算 / オンボーディング・認証 / 通知配信 / マスタ管理 の 8 コンテキストに確定。詳細は [07-bounded-contexts.md](./07-bounded-contexts.md) |
| 論点46 | サブドメイン分類（Core/Supporting/Generic） | Core: 自動分類・学習 / 家計分析 / 残高・資産推移管理 / 経費精算。Supporting: 取引取込 / オンボーディング・認証 / マスタ管理。Generic: 通知配信。詳細は [07-bounded-contexts.md §2](./07-bounded-contexts.md) |
| 論点47 | ユビキタス言語の記述形式 | kawasima ドメイン記述ミニ言語を採用。プロジェクト固有方言は spec §2.3。詳細は [08-ubiquitous-language.md](./08-ubiquitous-language.md) と 08a〜08h |
| 論点48 | 集約ルート候補 | 各コンテキストにつき 1〜4 個、計 21 個の集約ルートを特定。詳細は [09-aggregates.md](./09-aggregates.md) |
| 論点49 | コンテキストマップ・関係種別 | 詳細は [architecture/context-map.cml](./architecture/context-map.cml)（Context Mapper DSL）と [07-bounded-contexts.md §4](./07-bounded-contexts.md) |

### A.5 Phase 3.5（UX/UI 設計）で解決した論点

| ID | 論点 | 決定 |
|---|---|---|
| 論点50 | ロール表記とニックネーム | 役割名を「夫／妻」から **Honey（夫役）／ Darling（妻役）** に変更。ユーザー集約に **ニックネーム属性**（per-user・任意・編集可・10 文字以内目安）を追加。表示はニックネーム優先、空欄なら Honey/Darling にフォールバック。詳細は [2026-05-01-phase3.5-ux-ui-design.md §3](../superpowers/specs/2026-05-01-phase3.5-ux-ui-design.md) |
| 論点51 | 別銀行貯蓄口座と NISA 口座の名称 | 別銀行貯蓄口座に **銀行名属性**（例: 「楽天銀行」）、NISA 口座に **証券会社名属性**（SBI証券／楽天証券／その他）を追加。per-user で所有者本人のみ編集可。オンボーディング Phase 2-B で初期入力、設定画面でいつでも変更可。詳細は [2026-05-01-phase3.5-ux-ui-design.md §14.2-3](../superpowers/specs/2026-05-01-phase3.5-ux-ui-design.md) |
| 論点52 | アプリ名 | Phase 3.5 時点では**未定**（無冠 or 「（仮）」表記で進行）→ OQ-37 として正式化し、**2026-07-06 に「わりまる」で確定** |
| 論点53 | ダッシュボード設計（KPI 構成・世帯/個人切替・ドリルダウン） | KPI 4 枚（今月支出 / 貯蓄残高 / NISA 積立原資 / 資産合計）、世帯/個人 2-way セグメント切替、経費(会社) は別タブ。詳細は [2026-05-01-phase3.5-ux-ui-design.md §5](../superpowers/specs/2026-05-01-phase3.5-ux-ui-design.md) |
| 論点54 | ナビゲーション構造 | ボトムタブ 4（ホーム / 取引 / 経費 / 口座）+ ヘッダ右上に ⚙️ 設定 + 月次レポートバッジ |
| 論点55 | 取引編集モーダルの学習設定 UI | A 案（シンプル統合）: 学習トグル + ネスト遡及チェック。学習 OFF で M-1 学習無効化、過去遡及 ON で J-3、既存ルール上書きで L-4 警告 |
| 論点56 | テーマカラー（per-user） | 妻 = ピンク濃 v6 / 夫 = パステルスカイ A3+。背景・KPI・カテゴリ色がテーマで色相差し替え |
| 論点57 | LINE 通知 5 種と Deep Link | CSV取込リマインダー / 月次レポート世帯 / 月次レポート個人 / OAuth失効 / フェイルセーフメール の 5 種、各 deep link マップを定義 |

## B. 残る論点（実装フェーズで詰める）

| ID | 論点 | 補足 |
| --- | --- | --- |
| OQ-1 （一部解決） | SMBC通知メール／Amazon 注文確認メールの実フォーマット調査・パースルール | (1) ~~利用通知・入出金通知・引落通知・返金通知、それぞれの本文構造を実メールから確定する~~ → **解決（2026-07-26、#415）: 実メール .eml 3 通で確定**。本文構造・抽出規則・対象月の導出規則は [08a §2.1 実メールの本文構造](08a-ul-取引取込.md) を正とする。パース対象は **カード利用 / 振込入金 / 口座引落の事前お知らせ の 3 種**。**銀行出金通知・カード返金通知は届いていない**ため パーサを持たない（語彙は温存）。⚠️ 銀行出金の用途判別（08d / OQ-21 の出金側）はメール起点では成立しない。対象月は本文に無いため **引落予定日の前月** として導出する（締め日パターンが夫婦で違っても成立。ただし 15 日締めでは引落合計がカバーする利用期間が暦月とズレるため、差額「不明」計上 #399 側で考慮が要る）。妻側の事前お知らせメールが観測できたら引落日を追記する。(2) ~~SMBC公式案内では通知対象外とされている支出（公共料金・通信料・ガソリン・ETC・10円以下 等）が実際には通知されるケースがあるかも実測~~ → **解決（2026-07-26、#415）: 通知対象外でも届くことがある**。カード利用通知の本文注意書き「一部の電子マネー利用については通知されません。(注)利用内容によっては通知される可能性がございます」が公式の裏付け（実測: 電気代は届く）。家賃が妻のメールに届くかは未確認。(3) ~~**経費精算入金の振込元名・摘要パターンを確定**（給与振込との区別ルール）~~ → **解決（2026-07-24、#52 / OQ-21）: 振込元名・摘要では区別できない**。給与も経費精算入金も同一の勤務先から同一の摘要で同一口座に入金されるため、入金日 + 金額の 2 シグナル方式に切り替えた（08d §入金用途判別）。(4) **Amazon 注文確認メールの本文構造**（商品名・カテゴリ・合計金額の抽出ルール）— 未解決 |
| OQ-2 ✅ | Gmail OAuth トークン失効時の再認可フロー | **解決**: 検知 = API 呼び出し失敗時 ／ 通知 = LINE 個人 DM のみ（メールフェイルセーフ対象外） ／ 再認可 UX = アプリ経由（LINE メッセージのリンク → アプリ → OAuth 再認可ボタン） |
| OQ-3 ✅(撤回) | スナップショットの保持期間・粒度 | **シナリオC で廃止**: 月末スナップショット概念そのものを廃止。ダッシュボード・月次レポート画面はリアルタイム集計に統一し、配信時点の値の凍結は「LINE 配信ログ」（送信 payload を json 保持）で代替。詳細は シナリオC §Phase 5 参照 |
| OQ-4 ✅(改訂) | カテゴリ追加・統合UI | **シナリオC で精緻化**: 機能 = 追加・名称変更・削除のみ（統合・階層化なし） ／ 削除時 = **配下取引の移動先カテゴリ + 費用区分をユーザーが選択**（規定4種 or 残存追加カテゴリから／費用区分は4値＋現状維持）。学習データは移動先にリマップ。経費種別の削除も同構造 |
| OQ-5 ✅(深掘り) | 自動分類の学習機構 | **シナリオC で大幅深掘り**: ① **完全個人別（F-1）**: 夫婦の学習データは別テーブル、波及なし。② **フィールド独立（T-2）**: 加盟店名 → カテゴリ／費用区分／経費種別 を別マッピング。費用区分は 4 値全て学習対象。③ **Amazon 特例（X-1）**: ~~加盟店名「AMAZON.CO.JP」は学習対象外、商品カテゴリをキーに学習する別テーブル~~ → **商品キー学習は取り下げ（2026-08-23 / OQ-18 改訂・#391・#572）**。突合タイムアウト 3 日双方向は残る。加盟店名を学習対象外とする扱いを続けるかは OQ-18 改訂どおり #391 で決める。④ **修正 UX**: 1 画面 3 軸編集、未分類軸赤マーカー、新規カテゴリその場で作成可（E-1）。⑤ **学習反映**: 即時（I-1）。⑥ **過去遡及**: 確認ダイアログ（J-3）、対象は本人の未分類取引のみ。⑦ **既存ルール上書き**: 「以後このルールに従いますか？」確認（L-4）。⑧ **学習無効化**: 「この加盟店は学習しない」チェック（M-1）|
| OQ-6 ✅ | 取り崩し（別銀行貯蓄口座出金）の入力UX | **解決**: 入力起点 = アプリの口座詳細画面から「取り崩しを記録」「残高を補正」ボタン（ユーザー主体） ／ 残高ズレへの気付き = 月次レポートに「別銀行貯蓄口座: X円（最終更新 N 日前）」を表示し、N 日以上更新なしならアラート色 |
| OQ-7 ✅ | 二重取込検出の同一性判定ルール | **解決**: メール起源 = Gmail message ID で判定 ／ ソース間突合（メールvs CSV）= 日付・金額・加盟店名の3項完全一致で重複判定。**注意**: カード利用日と CSV 計上日が数日ずれる場合は別取引として両方残るため、計上のずれが起きうる（実運用で問題が出たら OQ-10 で詰める） |
| OQ-8 ✅(改訂) | 月次レポート生成タイミングと SMBC引落確定通知の到来順序 | **シナリオC で改訂**: ~~暫定生成（毎月1日 内部処理のみ）~~ は廃止 ／ 引落確定後 → CSV 取込でレポートが「CSV確定」状態に昇格 → このタイミングで LINE 配信 ／ 経費精算入金（翌月15日頃）後は内部で「最終確定」に昇格するが配信せず、差分は次月レポートで吸収。月次レポートは 2 状態（CSV確定／最終確定）に簡素化 |
| OQ-9 △ | カード／銀行 明細CSV／PDF のフォーマット調査・取込仕様 | **SMBC カード CSV は調査済み**（`samples/smbc-card/202605.csv`）: エンコーディング = **SJIS (CP932) 確定**、ヘッダなし、13列（利用日／利用店名／利用者／支払方法／空／支払月／利用金額／国内取引額／空／海外利用額／通貨コード／為替レート／換算日）、利用日降順、半角カナ・全角英数字混在。海外取引は列10-13 が埋まる別パターン。**SMBC 銀行 CSV も調査済み**（2026-07-24、`samples/smbc-bank/`）: エンコーディング = **SJIS (CP932)**、改行 = **CRLF**、**ヘッダ行あり**（カード側は無しのため取込側で分岐が要る）、**7 列**（年月日／お引出し／お預入れ／お取り扱い内容／残高／メモ／ラベル）、日付は `YYYY/M/D` で**ゼロ埋めなし**、金額はカンマなし整数・符号なし、**入出金は「お引出し」「お預入れ」の 2 列で表現し使わない側は空文字**（1 列符号方式ではない）、各行に**取引後残高**を持つ（カード側にはない）、日付降順。`お取り扱い内容`・`メモ`・`ラベル` は常にダブルクォート、日付・金額・残高は非クォート。摘要は半角カナ + 全角スペース(U+3000) 混在で、**桁数上限で末尾が欠落する**（`振込サービス　ｴｲ ﾌｵ-ｽ(ｶ` = エイフォース(株) の「株)」が切れている）。同一振込元でも空白位置が揺れる（`ｴｲ ﾌｵ-ｽ(ｶ` / `ｴｲﾌｵ-ｽ (ｶ`）ため OQ-7 の正規化（NFKC + 長音統一 + 空白圧縮）が銀行側でも必須。**未調査**: 加盟店名の標準化（半角カナ / 全角英数字混在のため）、エラー時のロールバック |
| OQ-23 ✅ | PDF → CSV 変換の実装方式 | **検証完了**: (1) `pdftotext` は使えない（CID フォントで抽出不可）→ Claude の vision/document 機能で PDF を直接処理する必要あり。(2) Claude Opus 4.7 で実サンプル（PDF 50 行）を CSV 形式に変換した結果、行数・利用金額合計（¥299,415）・構造（13列）が完全一致、加盟店名は **NFKC正規化＋空白圧縮＋長音統一** 後 **96% (48/50) 一致**。**実装方針（最終）**: ① **Lambda 内で Anthropic SDK（Claude API）を直接呼び出し**（subprocess 不要、Lambda 制約と整合、月数件で課金 ~30円） ② プロンプトに 13 列構造＋「空白・長音記号を忠実に」を明示 ③ 取込側で加盟店名に正規化関数（NFKC + 長音統一 + 空白圧縮）を適用して既存取引と突合（OQ-7 の3項一致判定で使用）|
| OQ-25 | ~~Claude Code のヘッドレス認証セットアップ手順~~ | **撤回**: 実装方式を Claude Code subprocess から Claude API に変更したため、認証セットアップ自体が不要 |
| OQ-26 | ~~Claude Code subprocess の出力パース方式~~ | **撤回**: Claude API の構造化レスポンスを直接パースするため、subprocess 出力パースが不要 |
| OQ-27 ✅ | アプリ実装形式と技術スタック | **解決**: アプリ形式 = LIFF ／ フロント = Next.js (TS) Static Export ／ バックエンド = Hono on Lambda (TS) ／ バッチ = EventBridge → Lambda（日次メール取込・月次バッチ） ／ DB = Neon Free Tier（PostgreSQL 互換、サーバーレス、月 0 円） ／ シークレット = Parameter Store（Secrets Manager より安価、家族用には十分） ／ 静的配信 = S3 + CloudFront（独自ドメインなし、デフォルト URL を使用） ／ HTTPS = ACM ／ PDF→CSV = Claude API 直接呼び出し ／ 月コスト目安: 30〜200円 |
| OQ-28 | AWS Lambda デプロイの実装詳細 | IaC 選定（CDK / SAM / Terraform / Serverless Framework）、CI/CD（GitHub Actions など）、リージョン選定（ap-northeast-1）、Lambda layer の使い方、デプロイパイプライン、ローカル開発環境（SAM Local / serverless-offline）|
| OQ-24 | PDF アップロード UX | スマホで画面印刷 → 共有メニューでアプリへ送信、または LINE → アプリリンク → アップロード画面 等。スマホからの主要動線設計 |
| OQ-10 | CSVと既存（メール通知由来）取引の突合ロジック | 同一性判定（日付＋金額＋加盟店名のハッシュ等）。誤マージ・取りこぼしを避ける |
| OQ-11 ✅(改訂) | 月次レポートの状態遷移仕様 | **シナリオC で改訂**: ~~暫定（毎月1日生成・配信なし）~~ を廃止し 2 状態に簡素化。CSV確定（CSV取込・分析完了で昇格・LINE配信トリガー）→ 最終確定（経費精算入金との突合で昇格・配信なし）。ダッシュボードはリアルタイム集計（暫定生成不要）|
| OQ-12 ✅ | LINE 通知の実装方式 | **解決**: LINE 公式アカウント（Messaging API）で共通トークルーム push と各個人 DM push を併用 ／ メッセージ形式は **使い分け**: リマインダー = プレーンテキスト（SMBC 明細ダウンロード URL ＋ アプリアップロード画面 URL を含む）／ 月次レポート = Flex Message リッチカード（サマリ数値 ＋ アプリリンク）／ グループ ID 取得手順とユーザー紐付けの具体実装は実装フェーズで詰める |
| OQ-13 ✅ | LINE リマインダー送信ルール | **解決**: 開始 = 毎月5日（前月分明細確定タイミング）／ 再送 = CSV 取込完了まで **毎日** ／ 解除 = CSV 取込完了で当月のリマインダー停止 |
| OQ-14 ✅ | フェイルセーフメールの送信トリガー定義 | **解決**: トリガーは **「LINE 連続送信失敗」のみ**（システム障害時の最終手段）。OAuth 失効や個別エラーは LINE で通知し、LINE 自体が止まった時だけメール経路へエスカレーション。N 回（具体値は実装で決定）／ 送信元（SMTP / SES）の選択は実装フェーズで決定 |
| OQ-15 ✅ | アプリの認証方式 | **解決**: LINE Login のみ ／ LINE userID = アプリユーザー ID として初回ログイン時に自動紐付け ／ Gmail OAuth はアプリ内「Gmail 連携」ボタンで別途取得（取込専用権限） |
| OQ-16 ✅(改訂) | アプリで明細を閲覧する画面の表示制御ロジック | **解決**: 「合計のみ」表示 = 合計金額のみ（件数・カテゴリ別は隠す） ／ 経費(会社) = 別タブ「経費精算管理」（本人のみ閲覧可） ／ 経費の月次累計表示 = 累計金額のみ（進捗バーなし） ／ **改訂（2026-08-23、判断セッション / #505）: 「進捗バーなし」を取り下げ、上限あり経費種別には当月累計に月次上限（上限金額）と進捗バーを併記し、上限到達済みはそれを明示する**（無制限経費種別は上限金額・進捗バーを出さず「上限なし」表示）。上限超過分は先着順で個人(本人) へ按分される（OQ-20）ため、使い切り具合が見えないと本人が気づかないまま個人負担になる。当初の狙い（上限を気にして経費フラグを付けなくなるのを避ける）よりこの不利益が重いと判断した。反映先: DESIGN.md §1 / 01-overview §6.6 / 02-event-storming §12.1 / packages/web/src/mocks/fixtures.ts（`docs/superpowers/` の Phase 3.5 成果物に残る旧方針は当時の記録として据え置く。DESIGN.md が優先する） |
| OQ-17 ✅(部分) | Amazon 注文と SMBC カード利用通知のマッチングロジック | **シナリオC で確定**: 突合タイムアウト = **3 日双方向**（先に来た側が後の側を最大 3 日待つ）／ ケース1: SMBC 通知先着で Amazon 注文未着 → タイムアウト後「Amazon 注文不明」フラグで未分類確定（費用区分はカード所有者の個人(本人) デフォルト）／ ケース2: Amazon 注文先着で SMBC 通知未着 → タイムアウト後注文情報を破棄（配送キャンセル可能性）。**未確定**: 同一性判定（金額完全一致＋日時の許容範囲）、複数注文が同日同金額の場合の扱いは実装フェーズで詰める |
| OQ-18 ✅(改訂) | Amazon 商品 → 経費種別の判別ルール | **シナリオC で方針確定**: ① **加盟店名「AMAZON.CO.JP」は学習対象外**（X-1）、商品カテゴリをキーに学習する別テーブル ／ ② 商品カテゴリ "本" → 新聞図書費 等のマッピングは memorization 学習（最初はユーザー手動分類、以降は自動）／ ③ 商品情報があっても判定不能な商品（"家電・コンピュータ"等）は「未分類」のまま手動分類待ち。**未確定**: Amazon JP の商品カテゴリ体系の標準化、商品名と商品カテゴリの粒度選択は OQ-35 で別途扱う ／ **改訂（2026-08-23、判断セッション / #391）: ① の X-1（商品カテゴリをキーに学習する別テーブル）を取り下げる**。2026-07-26 の実メール観察で、**Amazon 注文確認メールに商品カテゴリは含まれない**ことが確認された（`text/plain` から取れるのは注文番号・商品名・数量・単価・合計）。商品カテゴリを取るには Amazon Product Advertising API か注文履歴のスクレイピングが必要で、いずれも「自動集計できる範囲だけ追う」という設計原則（01-overview）から外れる。商品キーを「商品名」に再定義して残す案も検討したが、繰り返し買う消耗品にしか効かず、使わない仕組みが残るほうが混乱のもとと判断した。② の memorization マッピングも ① と一体のため取り下げる。③（判定不能な商品は未分類のまま手動分類待ち）は**そのまま残る** — Amazon の取引は当面すべてこの扱いになる。**加盟店名「AMAZON.CO.JP」を加盟店学習の対象外とする扱いも取り下げ、通常の加盟店学習に戻す**、としていたが、**再改訂（2026-08-26、判断セッション / #581）: 除外は取り下げず維持する**。理由: Amazon は買うものの種類がばらばら（本・日用品・家電…）で、店名一律の学習はかえって誤分類を増やす。未使用コード・テーブル・docs の整理は #572、突合エンジン本体（商品名の紐付けまで）は #391 で実施する |
| OQ-19 ✅ | AI 利用費の個人別月次上限値の確定 | **解決**: ベース上限 = 夫 10,000円 / 妻 3,000円 ／ 案件等での一時的な追加請求は **既存の「経費種別の上限額が変更された」機能** で対応（永続変更扱い、必要に応じて元の値に戻す）／ 月別オーバーライド機能は採用しない ／ **追加決定（2026-07-25）: 規定経費種別の残り 4 種の seed 上限も確定**。ジム・新聞図書費 = **ロール共通 5,000円**（Honey / Darling とも同額）、交通費・その他経費 = **無制限**（論点15 の無制限表現。上限運用の対象外）。あわせて seed の実行契機を確定: 論点14 の「Phase 1 の役割で自動紐付け」に従い、**役割確定（`RoleJudged`）を契機に規定経費種別ぶんの月次上限を自動作成する**（DB 初期化時の seed では実 LINE ユーザーID が未確定のため投入できない） |
| OQ-20 ✅ | 経費種別の月内按分ルール | **解決**: 分割アルゴリズム = **先着順**（月内の経費取引を時系列順に上限まで経費(会社)に充当、超過分を個人(本人) に分割）／ 取引修正時 = **自動再計算**（経費 ↔ 個人切替・種別変更・削除のたびに当月の按分を再評価） |
| OQ-21 ✅ | 経費精算入金の判別ルール | **解決（2026-07-24、#52）: 入金日 + 金額の 2 シグナル方式**。前提として、実調査で「振込元名では判別できない」ことが判明（#52、`samples/smbc-bank/`）。勤務先からの入金は給与・経費精算のいずれも `振込サービス　<勤務先名カナ>` という同一の摘要で入り、CSV の 7 列に種別を示す情報はない（`メモ`・`ラベル` は常に空文字）。観測サンプルでは同一振込元から 3 万円台 2 件と 30 万円台 1 件（月末）が混在。加えて空白位置の表記ゆれと桁数上限による末尾欠落があるため、摘要の完全一致によるルール化も不可（OQ-7 の正規化が前提になる）。給与も同一口座に入金されることを確認済みのため「勤務先からの入金 = すべて経費精算」とする逃げ道はない。また摘要が同一であるため **OQ-18 の memorization 学習では原理的に自動化できない**（同一キーに 2 つの答えが対応してしまう）。**確定したルール**: ① **入金日シグナル** — 月内基準日（既定 21 日）以降なら給与寄り、以前なら経費精算寄り。給与支給日 = 月末、経費精算 = 翌月 15 日頃（08e 責務）で、月内では常に経費精算が先に来る。給与日は土日祝で前倒しされ固定日にならないため、日ではなく窓で持つ。② **金額シグナル** — 25 万円以上なら給与寄り、未満なら経費精算寄り（既存の閾値を据え置き）。③ **2 シグナルが一致したときのみ自動確定**し、矛盾する場合（例: 月内上中旬の 25 万円以上 = 大型立替の精算か給与の前倒しか判別できない）は `用途不明`（暫定処理 = **手動確認待ち**）としてユーザーの判別を待つ。**旧ルールからの変更点**: 判別軸が「金額のみ」から「入金日 + 金額」になった点と、暫定処理を「25 万円以上は給与とみなす」から「手動確認待ち」に反転させた点。旧処理は誤判別を黙って確定させるため、経費精算を給与と誤判定すると突合が発火せず月次レポートが最終確定に昇格しなくなる。反映先: 08d §入金用途判別 / 04-scenario-a / 07-bounded-contexts / 10-strategic-design-validation。OQ-1(3) と連動 |
| OQ-22 ✅ | 月次レポートの状態遷移と LINE 配信タイミング | **解決**: CSV確定状態の昇格時のみ LINE 配信（暫定・最終確定では配信なし）。OQ-8/OQ-11 と整合 |
| OQ-29 ✅ | シナリオ B Phase 2-F: 当月途中（未確定）の CSV/PDF が SMBC マイページで取得可能か | **解決（2026-07-24）: 取得可能**。7/24 時点で 7/21 の取引まで含む CSV をダウンロードできた（入出金は即時反映される旨が明細画面にも明記）。したがって Phase 2-F を前月分のみに限定する / 全面スキップする必要はない。ただし **CSV は「月」ではなく「画面に表示中の範囲」をそのまま出力する**: SMBC ダイレクト SP サイトの明細画面は既定で**当月 + 前月の 2 ヶ月**を表示し、「さらに 3 ヵ月表示」で範囲が広がるとその分 CSV も伸びる。月単位で切り出す手段はない。**帰結**: 毎月取り込むと前月分が必ず重複して入るため、OQ-7 の重複判定（日付・金額・摘要の一致）が銀行 CSV では必須要件になる（カード側のように「その月だけ」を前提にできない）。OQ-9 / OQ-30 と連動 |
| OQ-30 | Phase 2-F の重複検出（CSV と日次バッチの境界） | Phase 2-F で取り込んだ CSV 取引と、運用開始翌日からの日次バッチが取り込むメール取引が重なった場合の扱い。OQ-7 の「日付・金額・加盟店名 3 項一致」+ Gmail message ID で吸収できるか実測 |
| OQ-31 | 日次バッチの「過去 5 日」の妥当性 | 5 日という値の根拠を実運用で検証。短すぎると Phase 2-F とのギャップを埋め切れない、長すぎると毎日のスキャンコストが増える。実装後に調整可能なパラメータとして持つ |
| OQ-32 | LINE 公式アカウントの管理アカウント | アプリ開発者（=自分自身）がデプロイ時に作成する LINE 公式アカウントの管理 LINE アカウント、Channel 設定値の管理者、フェイルセーフメールの送信先などの整理 |
| OQ-33 | Phase 2 設定変更履歴の保持 | 初期残高修正・カテゴリ追加・経費種別追加・月次上限変更などの設定変更履歴をログとして残すか。月次レポートとの整合（過去月のレポート再生成時に当時の設定値を使うか）|
| OQ-34 | LINE 配信ログテーブルのスキーマ・保持期間 | スナップショット廃止に伴って新設する LINE 配信ログテーブルの実装詳細。① スキーマ（payload json、配信タイミング種別、配信先、送信結果ステータス、送信日時等）／ ② 保持期間（永久保持 vs N 年でパージ）／ ③ 冪等性キー（同月レポートを再送信した場合の重複検知）／ ④ アプリ画面での閲覧 UI を持つかどうか（基本は LINE 側で見る前提だが、フェイルセーフ用に管理画面が要るか）|
| OQ-35 ✅ | Amazon 商品カテゴリの粒度・標準化 | **解決（2026-08-23、判断セッション / #391）: 対象外**。本論点は X-1（商品カテゴリをキーに学習）が前提だったが、その X-1 を OQ-18 の改訂で取り下げたため、カテゴリ体系の粒度・標準化を決める必要が無くなった。前提が崩れた経緯: 2026-07-26 の実メール観察で **Amazon 注文確認メールに商品カテゴリが存在しない**ことが判明した（起票時は「注文確認メールの本文で公開されているカテゴリ表記」を前提にしていたが、実物には無かった）。連動していた OQ-1(4)（Amazon 注文確認メールの本文構造）は同じ観察で解決済みで、そちらの成果（`text/plain` を正として注文番号・商品名・数量・単価・合計を抽出する）は #391 の突合エンジンで使う |
| OQ-36 | 学習データのパージ戦略 | 学習データが長期運用で肥大化した場合の対応。① 「学習しない」フラグ付きルールを保持し続けるか自動削除するか／ ② 長期間（例: 1 年）参照されていないルールを GC するか／ ③ 加盟店名の表記揺れ（ｺｰﾅﾝ / コーナン）で別ルール扱いになる場合の正規化（OQ-7 既存ルール NFKC + 長音統一 + 空白圧縮の流用） |
| OQ-37 ✅ | アプリ名の確定 | **解決（2026-07-06）: アプリ名 = わりまる**。Phase 3.5 候補（ふたりばこ / ふたり家計 / おうち家計 / わたしたちの家計 / Coinic / イエカケイ）とは別の名称で確定。「（仮）」の実表記はリポジトリ内に存在しなかったため置換対象なし（メタ記述のみ）。パッケージ名は OQ-45 で `@warimaru/domain` にリネーム済み |
| OQ-38 ✅ | SMBC 通知メールのフォーマット実調査 | **解決（2026-07-22）: 実メールで 3 種類のフォーマットを確認**。① **カード利用通知**（送信元: `statement@vpass.ne.jp`、件名: `ご利用のお知らせ【三井住友カード】`）— パース対象。`ご利用日時：YYYY/MM/DD HH:MM` + `店名（区分）\t金額円` の固定フォーマット。② **口座引落事前通知**（送信元: `SMBC_service@dn.smbc.co.jp`、件名: `三井住友銀行】口座引き落としの事前お知らせ`）— 引落日・口座・金額・内容がパース可能だが当面対象外。③ **送金受付通知**（同送信元、件名: `ことら送金受付完了のお知らせ`）— 金額を含まないため対象外。#76 の決定でメール取込の対象は①カード利用通知のみとした |
| OQ-39 ✅ | LINE Flex Message のサイズ制限と各人別リンク同梱 | **解決（2026-07-24）: 収まる。簡略版 + Deep Link へのフォールバックは不要**。Phase 3.5 spec §12.1 の通知 5 種のうち Flex Message 経路の 4 種（⑤フェイルセーフメールはメール経路のため対象外）について、§12.2 の Deep Link と §12.3 の構造で代表 payload を組み立て、push リクエストボディ全体の UTF-8 バイト数を実測: ①CSV取込リマインダー（「Honey 用 / Darling 用」×（三井住友カード・三井住友銀行）= 4 リンク + アプリ導線の計 5 ボタン同梱）**2,023 B** ／ ②月次レポート世帯 **2,636 B** ／ ③月次レポート個人 **1,413 B** ／ ④OAuth 失効 **996 B**。最大でも 50KB 上限の 5.2%、OQ-39 が懸念した 4 リンク同梱でも 4.0%。表示原則（01-overview §6.4「LINE に明細を載せない」）により本文はサマリ + リンクのみで、取引件数に比例して伸びる要素を持たないため、サイズは構造的に上限へ近づかない。参考として carousel 上限 10 バブルを②相当の密度で埋めた理論最悪ケースでも約 24.7KB。※ **上限値は公式ドキュメントで確認済み（2026-07-25、#52 の後追い検証）**: carousel を定義する JSON の最大サイズは **50KB**、carousel に含められる bubble は **最大 10**（Messaging API reference の Carousel: 「Bubbles in the carousel. Max: 10 bubbles」）。解決時点では実行環境のネットワークポリシーが `developers.line.biz` を遮断しており直接参照できず、上限 50KB は起票時の記載を踏襲・bubble 上限は 12 と記載していたが、いずれも本確認で 50KB は正・bubble 上限は 10 に訂正した（理論最悪ケースも 29.6KB → 約 24.7KB に再計算。50KB 以内のため結論は変わらない） |
| OQ-40 | テーマカラーのユーザー切替対応 | **解決予定: Phase 6（UI 実装時に判断、それまで保留）**。Phase 3.5 spec §13.1 で設定画面に placeholder のみ。実装するか・複数テーマを提供するかは UI 実装を見て判断。最低限「Darling 標準ピンク / Honey 標準ネイビー」の 2 種固定で運用開始する案を仮置き |
| OQ-41 ✅ | ID 生成方式（ULID か UUID v7 か） | **解決（2026-07-06、M-B spec §2.3）: ULID を採用**。内部発番 ID（24 種）は `shared/ids.ts` の正規表現を `^[0-7][0-9A-HJKMNP-TV-Z]{25}$` に強化済み（先頭桁 0–7 制限で 128bit 範囲外を排除）。外部由来 ID（UserId / TalkRoomId / LineMessageId / GmailMessageId / AmazonOrderId / SettlementNoticeId の 6 種）は形式が発行元依存のため `min(1)` を維持。生成は adapter 層（`ulid` npm パッケージ）、DB カラム型は `text`（PK） |
| OQ-42 ✅ | ドメインイベントの永続化要否 | **解決（2026-07-06、M-B spec §2.6）: 永続化しない**。イベント（M-A で型定義した 89 種）は in-process pub/sub のみで流す。家計内 2 ユーザー規模でリプレイ要件・外部購読者が存在せず、監査が必要な領域は集約側で監査レコードとしてモデル化済み（LINE配信ログ等）。将来要件が出た場合は outbox テーブル 1 本の追加で撤退可能 |
| OQ-43 ✅ | Repository.save() のトランザクション境界 | **解決（2026-07-06、M-B spec §2.5）: save 1 回 = 1 トランザクション**（集約 1 つの書き込み単位 = 整合性境界）。集約をまたぐ更新はアプリケーション層で順次保存 + in-process ドメインイベントによる結果整合。Saga / outbox は導入せず、途中失敗は冪等なイベントハンドラの再計算で自己修復。トランザクションを要する save は WebSocket `Pool`（Neon HTTP ドライバは interactive transaction 非対応） |
| OQ-44 ✅ | 鮮度アラート閾値の実値確定（OQ-7 続） | **解決（2026-07-24）: 閾値 = 35 日、通知先 = 画面表示のみ**。評価対象は「別銀行貯蓄口座（`Account.kind = 'other_savings'`）の手入力残高が最終更新から N 日経過」であり、メール／CSV の最終取込ではない（`daysSinceLastUpdate` を持つのは `other_savings` のみ。他 3 種は自動更新のため鮮度評価の対象外）。**35 日の根拠**: 別銀行貯蓄残高の更新は毎月 5 日の CSV取込リマインダーに合わせた月次運用を想定しており、正常時の更新間隔は 28〜31 日。30 日仮置きのままだと正常な月次サイクルの境界で毎月数日アラートが点灯する（フラッピング）ため、1 サイクル + 数日の猶予を取る。1 ヶ月飛ばした場合は約 60 日経過で確実に検知できる。**通知先**: 08c `behavior 残高鮮度を評価する` が定める「月次レポート画面で表示色を切り替える」に揃え、LINE 配信は行わない（08g の配信用途 5 種は変更なし）。実装は家計分析の Query で鮮度状態を付与する形で行う（**実装済み（2026-07-26 / #396）**: 閾値の単一ソースは `BALANCE_FRESHNESS_THRESHOLD_DAYS`、評価は `evaluateBalanceFreshness`、付与は `DashboardQuery.fetchBalanceFreshness`。残高一覧・月次レポート画面がこれを表示する。`AccountBalanceListView` は 08d L244 のとおり最終更新日時のみを供給する） |
| OQ-45 ✅ | パッケージ名のスコープ | **解決（2026-07-06）**: OQ-37 のアプリ名確定（わりまる）を受けて `@household/domain` → `@warimaru/domain` にリネーム済み。以後のパッケージは `@warimaru/*` スコープで統一する |
| OQ-46 ✅ | 技術スタック記述の不整合解消 | **解決（2026-07-06）: OQ-27 を正とする**。スタック = フロント Next.js (TS) Static Export ／ バックエンド Hono on Lambda ／ DB Neon (PostgreSQL) ／ 静的配信 S3 + CloudFront ／ シークレット Parameter Store。食い違っていた Phase 4 spec §13.1〜13.2（React + Vite ／ DynamoDB vs RDS 選定）は修正済み |
| OQ-47 ✅(改訂) | プライバシー3段階の強制レイヤ（Q-D の実装層確定） | **解決（2026-07-22、判断セッション / #86）: Query/API 層で完全強制する**。Static Export / LIFF 構成では UI 層マスキングがセキュリティ境界にならないため、配偶者に不可視のデータは API レスポンスに含めない（UI での null 化・マスキングに依存しない）。① 月次レポート Query は `viewerId` を用いて非本人の経費(会社)合計（`businessExpenseTotal*`）を除外。② 取引一覧 Query は配偶者の個人取引（`personal_honey/darling`）明細行を**完全除外**し、相手には合計のみを返す（従来の「店名・金額を null 化した伏せ字行」を廃止）。従来の spec §6.3⑦「経費(会社)サマリのマスキングは画面層の責務」および `applyPrivacyFilter` の伏せ字行挙動を上書きする。／ **改訂（2026-07-23、判断セッション / #108）: 除外対象を月次レポートの未承認振替（`unapprovedTransfers`）まで拡張**。未承認振替は「不認定になった経費を個人費用へ振り替えた額」で経費由来データのため、非本人の未承認振替も配偶者ビューから除外し、本人ビューでのみ表示する |
| OQ-48 ✅ | 個人費用区分と所有者役割の整合ガードの要否（#86 から分割） | **解決（2026-07-23、判断セッション / #107）: 整合ガードを入れる**。取引の所有者役割（Honey/Darling）と個人費用区分（`personal_honey` / `personal_darling`）が一致しない登録をドメイン層で弾く。相手名義の個人費用を代理入力する運用は無いことを確認済みで、不一致は取り違え（集計ズレ）でしかないため。「所有者役割」と「費用区分」をまたぐ規則のためドメイン層（services 等）に不変条件として置き、API 層では再実装しない。従来受け付けていた不一致入力を拒否する挙動変更は許容する |
| OQ-49 ✅ | 経費精算 最終確定の不変条件の配置と「不認定分あり・振替0件」の扱い（PR #99 の DDD レビュー見送り分） | **解決（2026-07-24、判断セッション / #100）: ドメイン層へ移す＋振替必須**。最終確定の検証（振替先=本人 / 振替合計=差額 / 精算入金を終端状態まで進める）を API 層からドメインの純粋関数へ切り出す（「経費サイクル」と「精算入金」の2集約にまたがる不変条件をドメイン層に置く、CLAUDE.md 方針に整合）。あわせて、差額 > 0（不認定分あり）のときは「振替合計 = 差額」を満たす振替を必須とし、満たさない確定はドメイン不変条件で弾く（精算入金が中間状態=突合済みに留まる不整合を原理的に防ぐ） |
| OQ-50 ✅ | フェイルセーフメールの送信失敗時の再送方針（#89 レビューから分割） | **解決（2026-07-24、判断セッション / #89）: 「発火済み」の記録は送信成功後に行う**。現状の「送信を試みる前に発火済みと記録する」実装では送信自体が失敗すると再送されず警告が握りつぶされるため、送信成功をもって発火済みとし、一時的なメール障害時は次回の LINE 連続失敗時に再送されるようにする。「宛先が空」「送信基盤の失敗」の2経路の扱いもこの方針で統一する。フェイルセーフは LINE 停止時の最終手段（OQ-14）であり、最終手段自体の失敗を無音にしないことを優先する |
| OQ-51 ✅ | 経費精算 最終確定で振替先に配偶者の費用区分を指定したときのエラー種別（PR #100 の DDD レビュー見送り分） | **解決（2026-07-24、判断セッション / #127）: 403（権限拒否）から 409（不変条件違反）に変更する**。振替先=本人の検証違反は「他人の集約への越権」ではなく「本人が区分を取り違えた=配偶者の個人支出合計がズレる整合性違反」であり、OQ-48（区分の取り違えは不変条件としてドメイン層で弾く）の整理と用語をそろえる。「差額なし＋配偶者区分の振替」で 403 が 409 より先に返っていた順序依存も解消される。利用者に見える挙動（操作が拒否されること）は変わらない |
| OQ-52 ✅ | マスタ削除リマップのイベント駆動化における物理削除のタイミング（#89 の実装設計） | **解決（2026-07-24、判断セッション / #89）: 集約役（コーディネーター）を置く**。削除確定でリマップ要請イベントを発火し、各コンテキスト（家計分析・自動分類学習・経費精算）の購読ハンドラーが付け替えを実行して完了通知イベントを発行する。コーディネーターが完了通知を集約し、**全コンテキストの完了がそろってから物理削除を実行する**。1つでも付け替えが失敗した場合は物理削除しない（マスタは残る）。同期発行のまま順次実行して最後に削除する案は、定義済みの完了通知イベントを実質使わず `08h`/`09-aggregates.md` のイベント駆動設計とズレるため不採用。実装は #223（分割は #89 コメント参照: #223 / #140 / #224 の3件）。**補足（2026-07-24、判断セッション / #223）: 実装方式は B（完了通知イベント新設）で確定**。コンテキストごとの「付け替え完了」通知イベント型を新設し（`08h` §3 の家計分析完了通知・自動分類学習完了通知等に対応）、イベントは発行元の各コンテキスト（家計分析・自動分類学習・経費精算）が所有、マスタ管理のコーディネーターが購読する。削除リクエストの状態に「完了したコンテキスト」の受け皿を追加する。イベント基盤は既存の in-process 同期バスのまま（新規インフラ不要を維持）とし、同期実行によりコーディネーターが実質順次処理になることは許容する（目的は構造を仕様に一致させ、将来の非同期化で作り直しを不要にすること） |
| OQ-53 ✅(改訂) | イベントチェーン2/3 の配線設計（#69 の実装設計） | **解決（2026-07-24、判断セッション / #69）: 4点確定**。① チェーン2（CSV 取込確定 → 月次レポート更新）の対象月特定は `CsvImportCompleted` に対象年月一覧を追加してイベントが運ぶ（既存購読者=通知リマインダー停止判定は追加項目を無視して従来どおり動くことを確認）。② 夫婦2人分の取引は `AppUserRepository.findByRole('honey'/'darling')` で両メンバーを列挙して合算する（世帯ロスターは新設しない。夫婦2人固定の前提を使う）。③ 月次レポートの残高由来フィールド（`balanceTrend` / `nisaContributionAccumulated` 等）はゼロ/空で初期化し、残高・資産推移側のチェーンが後から埋める。④ チェーン3（未払い記帳 → 口座残高更新）は「カード利用取引を取り込んだ」「引落確定通知を受信した」を表す**トリガーイベントを新設**し、メール取込バッチ（#35）が発行 → 購読ハンドラーが `bookUnpaid` / `settleUnpaid` を呼び結果イベント（`UnpaidBookkept` / `UnpaidSettled`）を発行する責務分割とする。識別子（未払金集約ID・口座ID 等）はトリガーイベントのペイロードに載せる／ **③ の改訂（2026-08-23、判断セッション / #398）: ゼロ/空初期化 + 後から埋める方式は成立しないため撤回し、残高の変動履歴を新設してそこを推移グラフの正とする（選択肢 B）**。③ は「残高・資産推移側のチェーンが後から埋める」と定めていたが、残高が動くのは月の途中（カード利用の記帳・引落の消込）である一方、書き込み先である当該月の月次レポートは翌月の CSV 取込確定ではじめて生成されるため、書き込み時点で行き先が存在しない。実際 `balanceTrend` に値を書き込むコードはリポジトリ内に 1 か所も存在せず、月次レポート画面の推移グラフは常に空・LINE 月次サマリからも残高 3 行（SMBC 残高 / 別銀行貯蓄 残高 / カード未払い）が欠落していた。改訂後は、残高が動くたびに変動履歴へ 1 行を残し（対象経路: カード利用の未払い記帳・引落の消込・銀行入出金の用途判別による残高更新・残高の手動操作・初期残高登録）、`BalanceTimeSeriesQuery` はこの履歴を直接読む。月次レポートの `balanceTrend` / `nisaContributionAccumulated` は**CSV 確定時点の値を残す凍結値**へ位置づけを変える（LINE の月次サマリはこの値を読む。実装時に「配信時点」ではなく「CSV 確定時点」で用語を統一した — 確定後・配信前に残高が動きうるため）。これに伴い DB 設計スペック（`docs/superpowers/specs/2026-07-06-phase5-m-b-db-schema-design.md` §4.2）の「専用の残高履歴テーブルは設けない／残高スナップショットの正は月次レポートに凍結済み」は #398 の実装で改訂済み（新テーブルは同 spec §4.2b `balance_history_entries`、集約は 09-aggregates.md #10c）。なお `docs/domain/04-scenario-a-monthly-cycle.md` の「月末スナップショット概念は廃止・リアルタイム集計に統一」とは本改訂で向きが揃う。履歴は**口座ごと**に残すこと（#406 の口座詳細画面が口座 1 つ分の推移を必要とするため。世帯 4 軸に丸めた形だけで作ると再実装になる） |
| OQ-54 ✅ | LINE 通知 Deep Link の URL 契約（#50 の実装設計） | **解決（2026-07-24、判断セッション / #50）: 既存ルート + クエリパラメータ方式で受け側（#50）が契約を確定する**。① URL 形式は既存ページ + クエリパラメータ（例: `/reports?month=YYYY-MM`、`/imports?month=YYYY-MM`、`/settings?section=oauth`）。パス形式の新ルート（`/report/[month]` 等）は Static Export と相性が悪く作らない。spec §12.2 の URL 表記は本決定に合わせて改訂済み。② 送り側（LINE 通知本文の URL 生成）は未実装で既定の契約が存在しないため、受け側が受理する形を正式契約とし送り側が従う。③ レポートのビュー切替（`view=household|personal`）は対象外とし、プライバシー3段階の表示設計を伴うため別 Issue で判断する（`needs-decision` 起票）。／ **③ の追加解決（2026-07-24、判断セッション / #276）: ビュー切替は作らない**。切り出しの理由だったプライバシー表示設計は OQ-47 で既に決着しており（Query/API 層で完全強制。配偶者に不可視のデータは API レスポンスに含めない）、`view` を受けなくても「相手の個人明細を見せない」「経費は本人のみ」は満たされる。レポート画面は既に世帯支出と個人・経費を1画面に併記しており、切替 UI を足しても新しい情報が増えないため、`view` パラメータは受理しない。将来 UX 上必要になった時点で改めて起票する |
| OQ-55 ✅(改訂) | LINE Webhook 受信の設計（follow / join。#73 の実装設計） | **解決（2026-07-24、判断セッション / #73）: 4点確定**。① join（共通トークルーム参加）は**世帯レベルの置き場所を新設**して1か所に記録する（per-user の `talkRoomJoin` への二重書き込みはしない。join Webhook が userId を含まず、参加は世帯にひとつの事実のため）。通知配信（`DeliveryTarget.shared_talk_room`）・`NotificationActivated` が参照する「正」はこの世帯レベルの記録。`recordTalkRoomJoined` / `LineOperationSettings` の再設計を伴う（08f / 09-aggregates に改訂注記あり）。② 自己申告 API（`/api/onboarding/phase1/line-friend` / `/phase1/talk-room`）は**廃止**し、セットアップ画面を「Webhook 由来記録の確認」へ改修する（Webhook 未検知時は案内表示で待機）。③ 登録前ユーザーの follow イベントは処理せずログのみ残し、**登録完了時に LINE の友だち状態照会で確認し直す処理を必須で実装**する（②により自己申告の逃げ道がないため）。④ 署名検証鍵 `LINE_CHANNEL_SECRET` は既存 LINE アクセストークンと同じ Parameter Store 経由（ローカルは環境変数）。Webhook ルートは gmail-oauth と同様 LIFF 認証（`/api/*`）の外に置く。／ **① の追加解決（2026-07-25、判断セッション / #334）: 通知有効化記録（per-user）が持つ共通トークルームID は廃止し、世帯レベルの記録に一本化する**。有効化記録は「いつ有効化したか」（有効化日時）のみを保持し、トークルームの情報はすべて世帯レベルの記録（#295 で新設）を参照する。`08f` の「有効化済み = 共通トークルームID AND 有効化日時」の定義は「有効化日時のみ」へ改訂する（改訂は実装 Issue #334 の PR で行う）。トークルーム作り直し時に per-user 記録へ古い ID が残って検知されない二重保持を解消するため／ **① の追加解決（2026-07-26、判断セッション / #371）: join Webhook で参加を記録する前に、招待されたトークルームに世帯のいずれかのユーザーが在籍しているかを Messaging API へ照会する（選択肢 A）**。join の source は userID を含まず、公式アカウントを自分のグループへ招待できる第三者も正規の join を発生させられるため、既存記録の上書き禁止だけでは「未参加のうちに第三者が先に招待する」初回の取り合いを防げない。在籍を確認できないとき（照会失敗・アプリユーザーが 1 人も未登録）は記録しない（fail-closed）。照会失敗は「在籍なし」と区別する。うち一時障害（LINE の 5xx・通信断・タイムアウト・鍵の解決失敗）は Webhook 自体を失敗として返し LINE の再送で回収し、恒久的な失敗（権限・設定不備、照会対象のアプリユーザーが未登録）は再送に回さず終端する（join は招待の瞬間にしか発生せず再送されないため、一時障害を握って 200 で返すと正しいトークルームへ招待した夫婦の配信先が永久に登録されない）。恒久的な失敗と「在籍なし」の回復は招待のやり直しか自己申告 API による。なお同セッションで併せて諮った「Webhook 署名検証鍵を数分キャッシュする」は回答が無く、鍵の実体をアプリ内に置かない既存方針を維持する（毎回 Parameter Store から解決する）ため未実装／ **②③ の追加解決（2026-08-22 / #417）: 友達状態照会が失敗した回の回復手段として、セットアップ画面に利用者が押せる「友達追加の確認」を置く（選択肢 A）**。登録要求に相乗りした照会は画面が初回しか送らないため、失敗した回は自己申告 API（②で廃止）以外に立て直す手段が無かった。確認結果は「友達未追加」と「照会不能」を区別して画面へ返し、次にとるべき行動（LINE で友達追加する／通信状況を確かめてやり直す）を出し分ける。なお同 Issue で併せて諮った「`follow_Webhook受信日時` を実態に合わせて改名するか」は回答が無く、名称は据え置きのまま判断待ち（#492） ／ **改訂（2026-08-23、判断セッション / #447）: 通知機能の有効化に、per-user の状態とは別に世帯レベルの「有効化日時」の記録を新設する**。#334 の整理（有効化の情報はひとりずつ持つ）は残すが、「運用開始のテストメッセージをもう送ったか」の判断を per-user の有効化状態の組み合わせからの推測ではなく、この世帯レベルの記録で行う。推測に頼る従来の作りでは、有効化の保存に成功して送信の依頼で失敗した場合に「もう送った」と見なされ、テストメッセージがその世帯へ二度と送られない（届かないことは画面にも出ず、気づく手段は運用ログのみ）。記録は**送信の依頼が成功して初めて書く**ため、失敗した回は次の操作でやり直せる。月次レポート配信も同じ作りを使っているため、#441 の決定が出た時点で記録の持ち方を揃えるか確認すること（#447 を先行実装する判断のため、作り直しの可能性は織り込み済み） ／ **改訂（2026-08-26、判断セッション / #590）: 世帯通知有効化記録を書くタイミングを、`NotificationActivated` の発行成功から LINE への配信確定までさらに一歩遅らせる（選択肢 A）**。#447 は「発行に失敗した回」は回復できるようにしたが、発行後に LINE への送信が最終的に失敗した回（配信サービスの `retry_abandoned`）は記録が残ってしまい回復できないままだった。通知配信からの `TestMessageSent`（配信確定、実送信または冪等スキップ）を検知して初めて記録するようにし、この残りのギャップを閉じる。「記録に送信先（トークルームID）も持たせ、共通トークルームの作り直しを検知して送り直すか」は本セッションでは回答が無く、判断待ちの Issue（#705）として切り出した |
| OQ-56 ✅ | SMBC 銀行口座・三井住友カード口座の本番登録経路（#395） | **解決（2026-08-23、判断セッション / #395）: 口座登録 API の種別拡張（選択肢 A）**。既存の `POST /api/accounts`（`RegisterBodySchema`）に `smbc_bank` と三井住友カード口座の種別を追加し、オンボーディング Section B の画面から利用者に登録してもらう。ドメイン側にも既存の `registerOtherSavingsAccount` / `registerNisaAccount` と同層の生成関数を追加する。不採用: 世帯作成時の自動生成（B。「口座をアプリに登録する」という 08d の UL 記述の書き換えが要る）／本番 seed で投入（C。登録経路がリポジトリ外の運用手順になる）。選定理由は、口座を作る経路が画面上で完結してリポジトリ外の運用手順に依存せず、UL の記述をそのまま保てるため（利用者の操作は 1 手増える）。決定前は `smbc_bank` 種別の行を生成するコードが開発用フィクスチャ（`packages/adapters-postgres/scripts/seed/dev-fixtures.ts`）にしか存在せず、本番環境ではオンボーディング Section B（初期残高登録）が完走できない状態だった |
| OQ-57 ✅ | Gmail 未連携／失効中の日次メール取込の扱い（#488 / #514） | **解決（2026-08-23、判断セッション / #488 + #514）: 記録の層とアラートの層を分けて扱う**。① **取込結果の記録**（#488）: Gmail 連携が無いユーザーは取込を起動せず「対象外」を結末として残す（選択肢 A）。連携の確認をバッチ起動記録（`ImportBatch`）の生成より**前**に行い、未連携なら起動記録も失敗記録も残さない。`HouseholdMemberMailImportResult` に既存の `not_registered` と同層の結末を追加する。08a のバッチ起動の事前条件「Gmail OAuth 連携を完了している」に実装を合わせるもので、定時起動下で失敗レコードが毎日積み上がり本物の失敗（通信断・失効）が埋もれるのを防ぐ。② **バッチ全体の成否**（#514）: 「対象外」があった回も**引き続き異常として上げる**（現状維持）。再認可導線（#392）が未実装（かつ #35 にブロックされている）ため、異常から外すと誰にも気づかれないまま取り込まれない日が続くから。**実装上の注意**: ① で未連携が「失敗」から「対象外」に変わるため、バッチの成否判定（`packages/api/src/batch/jobs.ts`。PR #515 が持ち込む）で「対象外」を成功として素通しさせないこと。素通しすると ② の決定が黙って裏返る。#392 が実装され再認可導線が機能するようになった時点で、② を「異常から外す」へ見直す余地がある |
| OQ-58 ✅ | 残高変動・月次レポートに載せる「時点」の定義（#539） | **解決（2026-08-23、判断セッション / #539）: 取引が実際に発生した日（カード利用日・引落日）を用いる**。#398 で入れた残高変動履歴の「いつ」は、カード利用の計上・引落の消込・引落による残高反映の 3 つで、イベントの発生日時が「処理を実行した日」になっていた。日次でメールを取り込んでいる限りズレは 1 日以内だが、取りこぼしを後からまとめて取り込むと取り込んだ月に記録され、資産推移グラフと月次サマリの月ごとの比較が意味を失う。グラフとサマリは「その月にいくら使ったか」を見るためのものであるという判断で、発生日側に寄せる。**帰結として、確定済みの過去月レポートに後から数字が足される場合がある**（配信済みの月次サマリと食い違いうる）ことを許容する |
| OQ-59 ✅ | 分類（カテゴリ・経費種別）削除の付け替え完了報告の受理条件（#520） | **解決（2026-08-23、判断セッション / #520）: 依頼した担当からの報告のみ受け付ける**。マスタ削除は「使っていた取引と学習ルールを別の分類へ付け替え、全担当（家計分析・経費精算・自動分類）の完了が揃ってから物理削除」の順序で進む（OQ-52）。依頼していない担当からの完了報告を受理すると、(1) 削除完了の記録に残る「移動した取引の件数」が実際より多くなり後から真偽を確かめられない、(2) 依頼先の集合と完了の集合の突き合わせが将来の変更で成立しなくなる。不変条件はドメイン層（削除の進み具合を持つ型）に置き、否定形テスト（受け付けないこと）を必須とする。OQ-48 / OQ-49 と同じ「取り違えは不変条件でドメイン層が弾く」整理に揃える |
| OQ-60 ✅ | 残高一覧に並ぶ口座の範囲と、世帯の資産合計の出し方（#453） | **解決（2026-08-23、判断セッション / #453）: 一覧は本人の口座のみ・総資産は世帯合計のまま**。決めごと（オンボーディング設計 P2-B5、受入シナリオ AT-404）は「残高は本人のみが見られる。別銀行貯蓄と NISA だけは合計金額のみ相手にも見せる」としていたが、実装は夫婦両方の口座を 1 件ずつ並べており、意図的にそう作られていた（読み取り処理に「残高は世帯共有なので閲覧者で絞らない」の注記）。**文書を正とし、実装を寄せる**。① 残高一覧は自分の口座だけを並べ、相手の分は「別銀行貯蓄 + NISA の合計」1 行にまとめる（相手の SMBC 残高・カード未払いは不可視になる）。② 画面上部の資産合計（当時の画面表記は「総資産」。#366 で用語に合わせて改名）は変更せず、世帯の合計金額と 4 つの内訳（SMBC 残高・別銀行貯蓄・NISA 積立累計・カード未払い）を出し続ける。資産推移グラフも変更しない。隠す狙いは「相手の口座を 1 件ずつ覗ける状態をなくす」ことであり ① で足りる一方、資産合計まで本人分に絞ると「世帯の資産合計」という数字自体が失われ、夫婦で家計をまとめて見るという目的とぶつかるため。OQ-47（プライバシー3段階は Query/API 層で完全強制）の適用範囲を残高一覧にも及ぼす形になる ／ **追加解決（2026-08-26、判断セッション / #594）: ①（一覧を本人のみにする）の裏返しとして、資産合計の内訳（SMBC 残高・カード未払い）と自分の口座の金額の差引きから相手の SMBC 残高・カード未払いが計算できる状態を許容する**。「口座を 1 件ずつ覗ける状態をなくす」という ① の狙いはこの状態でも満たされており、資産合計まで本人分に絞ると「世帯の資産合計」という数字自体が失われるという ② の理由と同じトレードオフのため、追加の実装は行わない |
| OQ-61 ✅ | 入金の自動判別に使う「勤務先の振込元名」の登録経路と保管先（#448） | **解決（2026-08-23、判断セッション / #448）: 利用者ごとに保管し、#390 の確定窓口で候補から選んで登録する**。① 登録経路は「実際に届いた入金から候補を出して選んでもらう」（2026-07-26 回答）。この機能はもともと「見分けがつかない入金は本人に確認する」前提のため、#390 で作った未確定入金の確定窓口で、確定時に「この振込元は今後も勤務先からのものとして扱う」を選べるようにする。設定画面での手入力は作らない。② 保管先は**夫婦ひとりずつ**。給与はひとりずつ受け取るもので、夫婦が別々の勤務先でも成り立つ形にしておく必要があるため（世帯にひとつだと別勤務先のとき片方の給与が毎回「確認待ち」に落ちる）。相手の勤務先の振込元名は相手には見えない。**この保管先が無いまま日次メール取込バッチ（#414 / #35）を繋ぐと勤務先からの入金がすべて確認待ちに落ちる**ため、バッチ稼働より先に実装する。OQ-21（給与と経費精算は同じ勤務先から同じ表示で振り込まれ名前だけでは区別できない）を受けた判定ルールの入口にあたる |

## C. 参考: 改訂で対象外とした初期論点

初版で「次回深掘り」として挙げていた以下は、改訂スコープでは対象外:

- ~~シナリオC（クレカ引落日）~~: 撤回後、**「未分類修正と memorization 学習」として 2026-05-01 にウォークスルー実施済み**（[06-scenario-c-classification-learning.md](./06-scenario-c-classification-learning.md)）。クレカ引落そのものは発生主義採用で資金移動として淡々と処理（変更なし）
- シナリオD（夫が立替→妻に精算）: 夫婦間精算は対象外
- シナリオE（自動連携が壊れる日）: メール通知のみのため "連携" の概念が単純化。OAuth失効は OQ-2 として残す
- 領域1（現金）: 完全に対象外
- 領域2（年単位イベント・確定申告）: 対象外
- 領域3（旅行・特別支出のグルーピング）: 対象外（カテゴリ拡張で将来対応の可能性は残す）

## D. 次の進め方

- ~~並行して Phase 3 へ進む: コンテキストマップ・集約候補・コマンド抽出~~ → **Phase 3 完了済み**（2026-05-01）
- 次フェーズ: Phase 4（実装計画）。実調査タスク（OQ-1, OQ-9, OQ-21, OQ-29）と並行して進められる

ディスカッション可能な OQ はシナリオ B/C のウォークスルーまでで概ね解決済み。残るのは実物調査または実装フェーズで詰める論点のみ。

1. **実調査タスク**（実物のメール・CSV を取得して確認）
   - ~~OQ-1: SMBC通知メール／Amazon 注文確認メール の実フォーマット調査~~ → **解決済み（2026-07-26）**。連動していた OQ-35（Amazon 商品カテゴリ体系）は、カテゴリがメールに存在しないことが判明し、X-1 ごと取り下げ（OQ-18 改訂 / #391）
   - OQ-9: カード／銀行 明細CSV の実フォーマット調査（カードCSV は `samples/` 内で確認済み、銀行CSVと PDF 仕様の細部のみ未確定）
   - OQ-21: 経費精算入金の判別ルール（OQ-1 と一緒に実調査）
   - OQ-29: 当月途中（未確定）の CSV/PDF が SMBC マイページで取得可能か

2. **実装フェーズで詰める**
   - OQ-10: CSV と既存取引の突合（細部、ファジーマッチへの拡張）
   - OQ-17: Amazon と SMBC利用通知のマッチングロジック細部（同日同金額の複数注文等、シナリオC で大枠は確定）
   - ~~OQ-18: Amazon 商品 → 経費種別の判別ルール~~ → **X-1 を取り下げ（2026-08-23 / #391）**。Amazon の取引は当面すべて手動分類待ちとなる。加盟店名 AMAZON.CO.JP による学習（`MerchantLearningRule` の `isAmazonMerchant` 除外）も除外を維持すると決定（**2026-08-26 / #581**）。Amazon は買うものの種類がばらばらで、店名一律の学習はかえって誤分類を増やすため
   - OQ-24: PDF アップロード UX（スマホからの動線設計）
   - OQ-28: AWS Lambda デプロイの実装詳細（IaC・CI/CD・リージョン）
   - OQ-30: Phase 2-F の重複検出（CSV と日次バッチの境界）
   - OQ-31: 日次バッチの「過去 5 日」の妥当性
   - OQ-32: LINE 公式アカウントの管理アカウント
   - OQ-33: Phase 2 設定変更履歴の保持
   - OQ-34: LINE 配信ログテーブルのスキーマ・保持期間（シナリオC スナップショット廃止に伴う新設）
   - ~~OQ-35: Amazon 商品カテゴリの粒度・標準化~~ → **対象外（2026-08-23 / #391）**。前提の X-1 を取り下げたため
   - OQ-36: 学習データのパージ戦略

> ※ OQ-25, OQ-26 は Claude Code subprocess 方式から Claude API 直接呼び出しに変更したため撤回
> ※ OQ-3 は シナリオC でスナップショット概念そのものが廃止されたため、保持期間・粒度の議論は不要となった
