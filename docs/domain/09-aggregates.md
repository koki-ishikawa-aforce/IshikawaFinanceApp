# 集約候補リスト

> 本ドキュメントは Phase 3 戦略的設計の集約候補成果物。
> 作成: 2026-05-01
> Task 5 で各コンテキストファイル（08*-ul-*.md）に `// aggregate root` を付けた data を集めて整理。
> Phase 3.5 反映（[2026-05-01-phase3.5-ux-ui-design.md §14](../superpowers/specs/2026-05-01-phase3.5-ux-ui-design.md)）: 口座集約に `銀行名`（別銀行貯蓄）/ `証券会社名`（NISA）追加。アプリユーザー集約に `ニックネーム` 属性追加。役割名を `Honey / Darling` 表記に変更。
> 改訂（2026-07-24・判断セッション / #73、OQ-55）: アプリユーザー集約（#14）の LINE_運用設定 から共通トークルーム参加状態を分離し、世帯レベルの置き場で保持する（詳細は 08f §1）。実装（#295）で世帯レベルの集約 **#14b 共通トークルーム** として新設した。通知配信（配信メッセージ #15 の共通トークルームID）・通知機能有効化が参照する「正」は #14b。
> 改訂（2026-07-25・判断セッション / #334、OQ-55 ①）: アプリユーザー集約（#14）の LINE_運用設定 のうち通知機能有効化状態からも共通トークルームID を除き、有効化日時のみを保持する。トークルームID を保持するのは #14b 1 か所（詳細は 08f §1）。
> 改訂（2026-08-23・判断セッション / #391・#572）: X-1（Amazon 商品キー学習）の取り下げに伴い、**#5 Amazon商品キー学習ルールを廃止**した。番号は後続集約の参照を壊さないため欠番として残す（詳細は 08b の冒頭注記）。
> 改訂（2026-08-23・判断セッション / #447、OQ-55 ① 改訂）: 世帯レベルの集約 **#14c 世帯通知有効化記録** を新設した。「運用開始のテストメッセージをもう送ったか」の判断は、アプリユーザー集約（#14）の通知機能有効化状態の組み合わせからの推測ではなく #14c で行う。#14 の per-user の有効化状態は #334 の整理どおり残る（詳細は 08f §1 §2）。
> 改訂（2026-08-23・判断セッション / #398、OQ-53 ③ 改訂）: 残高・資産推移管理に **#10c 残高変動履歴エントリ** を新設した。資産の推移グラフが読む正はこの集約で、月次レポート（#8）の残高推移パート・NISA積立累計は CSV 確定時点の値を残す凍結値に位置づけを変えた（詳細は 08d §1）。
> 改訂（2026-08-26・判断セッション / #684・#688、docs-drift）: 実装との乖離解消 2 件。**#14d Gmail_OAuth_トークン** を独立した集約行として追記した（実装は当初から集約として存在し専用 Repository を持つ。詳細は 08f §1）。配信メッセージ（#15）の不変条件に「配信用途×配信先の整合」を追記した（`DeliveryMessage` の superRefine が強制する既存実装の明文化）。
> 改訂（2026-08-28・docs-drift / #685）: 取引候補（#1）の状態 OR に、実装済みで docs 未記載だった「確定済み取引候補」を追記した（取引候補から実際の取引を生成した後、同じ候補を再利用不可にする終端状態。実装は当初から存在。生成された取引への参照（取引ID）を外部参照列にも追記した）。
> 改訂（2026-08-28・判断セッション / #651、決定 A-1）: 集約 **#14e アクセス拒否カウンタ** を新設した。見知らぬ相手からのアクセス拒否（許可リスト不一致）を LINE_userID ごとに集約する。従来サーバログにのみ残っていた拒否の事実を、累計回数・最終発生日時のみを保持する形で記録する（個々の発生時刻の履歴は残さない。詳細は 08f §1 §2）。
> 改訂（2026-08-29・docs-drift / #689、決定 A）: カテゴリマスタ（#18）・経費種別マスタ（#19）の不変条件に「規定/追加とスコープの対応」を追記した（実装（`CategoryMaster.ts`・`ExpenseTypeMaster.ts` の `superRefine`）は当初から本制約を強制し、一次資料の 08h にも既に記載済みだったが、本ファイルの表にのみ反映漏れがあった）。
> 改訂（2026-08-28・docs-drift / #686）: 加盟店学習ルール（#4）の不変条件に「加盟店名『AMAZON.CO.JP』は加盟店学習の対象外」を追記した（実装は当初から `superRefine` で強制済みで、一次資料の 08b にも既に記載済みだったが、本ファイルの表にのみ反映漏れがあった）。
> 改訂（2026-08-28・docs-drift / #687）: 取引（#7）の不変条件に「個人(夫)・個人(妻) の費用区分は所有者ユーザーIDのロールと一致」を追記した（実装（`PersonalExpenseClass.ts` の `assertPersonalExpenseClassMatchesRole`）は当初から本制約を強制していたが、一次資料 08c にも本ファイルにも記載が無かったため両方に追記した）。

## 1. 集約候補リスト

`grep -B1 "// aggregate root" docs/domain/08*-ul-*.md` で 21 個の集約ルートを確認（作成時点の本数）。その後 #10b・#10c・#14b・#14c・#14e を追加し、#5 を廃止（欠番）、#14d（実装当初から集約だったが行が無かった Gmail_OAuth_トークン）へマーカーを追記したため、現在このコマンドが返すのは 25 個。

| # | コンテキスト | 集約ルート | 不変条件（同一トランザクションで守るべき） | 集約内に含める data | 外部参照（ID のみ） |
|---|---|---|---|---|---|
| 1 | 取引取込 | 取引候補 | パース失敗時は取引候補が生成されない / Gmail_message_ID 重複時は新規取引候補を生成しない（重複除外の閉包） | 取引候補共通属性（取引候補ID・ユーザーID・取込ソース・加盟店名・金額・発生日時）、通常取引候補／Amazon突合取引候補／突合タイムアウト未分類候補／確定済み取引候補 の状態 OR | Gmail_message_ID、アップロードファイルID、Amazon注文ID、取引ID（確定済み取引候補が参照する生成済み取引） |
| 2 | 取引取込 | 日次メール取込バッチ | バッチ起動中は同一ユーザーID の二重起動が発生しない / 完了・失敗の終端状態からは遷移しない | バッチ共通属性（バッチID・ユーザーID・バッチ起動日時・取込対象期間）、起動済み／取込中／完了／失敗 の状態 OR、取込件数・重複除外件数・失敗件数 | ユーザーID |
| 3 | 取引取込 | 明細取込ジョブ | PDF → CSV 変換中は同一ジョブID で二重変換が発生しない / 状態遷移は単方向（後戻りなし） | ジョブ共通属性（取込ジョブID・アップロード者ユーザーID・取込対象月・ファイル種別・ファイル参照）、アップロード受付済み～完了／失敗 の状態 OR、取込結果サマリ | アップロードファイルID、PDF変換ジョブID |
| 4 | 自動分類・学習 | 加盟店学習ルール | F-1: ユーザーID ＋ 加盟店名で一意（同一ユーザーのルール重複なし） / T-2: カテゴリ・費用区分・経費種別を独立した参照として保持 / 有効ルールと学習無効化が同時に存在しない / 加盟店名「AMAZON.CO.JP」は加盟店学習の対象外（X-1 商品キー学習の取り下げ後も維持。OQ-18 改訂 / #581） | ユーザーID、加盟店名、カテゴリ参照・費用区分参照・経費種別参照（各フィールド独立）、最終更新日時、有効／学習無効化 の状態 OR | カテゴリID、経費種別ID |
| 5 | ~~自動分類・学習~~ | ~~Amazon商品キー学習ルール~~ | **廃止（2026-08-23 / #391・#572）**: X-1 取り下げにより集約ごと削除。番号は欠番 | — | — |
| 6 | 自動分類・学習 | 一括分類セッション | ユーザーID ＋ 取込起因で 1 セッションに限定（進行中セッションの二重起動なし） / 完了・中断状態からは再開しない / 進行中の 残件数 = 対象取引数 - 分類済み取引数 / 保存は版数照合で行う（読み出したときの版と一致するときだけ書き込み、不一致は拒否して後勝ちを防ぐ。#609） | ユーザーID、取込起因（CSV取込起因／単発修正起因／取引一覧起因）、List<未分類取引>、一括分類状態（進行中（分類済み取引IDを保持）／完了／中断）OR、版数（#609） | 取引ID、CSV取込ID |
| 7 | 家計分析 | 取引 | 費用区分が4値のいずれかに限定される / 経費(会社) の場合は経費種別必須（経費種別参照 = 経費(会社)取引 なら 経費種別ID 必須） / 削除済み取引は変更不可 / 個人(夫)・個人(妻) の費用区分は所有者ユーザーIDのロールと一致（個人(夫)↔夫(honey) 所有、個人(妻)↔妻(darling) 所有。世帯・経費(会社) は対象外） | 共通取引属性（取引ID・所有者ユーザーID・加盟店名・金額・発生日時・取込ソース）、未分類取引・分類済み取引・削除済み取引 の状態 OR、分類済み取引の場合は カテゴリID・費用区分・経費種別参照・分類根拠 | カテゴリID、経費種別ID、Gmail_message_ID、CSVファイルID、Amazon注文ID |
| 8 | 家計分析 | 月次レポート | CSV確定 → 最終確定 の単方向遷移のみ許容（最終確定状態からは戻れない） / 最終確定サイクルには経費精算入金参照が必須 | 月次レポート共通属性（月次レポートID・対象年月・各費用区分合計・NISA積立累計・残高推移パート・不完全月フラグ）、CSV確定月次レポート・最終確定月次レポート の状態 OR、不認定分振替リスト | 経費精算入金ID、取引ID（CSV確定起因取引リスト） |
| 9 | 残高・資産推移管理 | 口座 | 同一ユーザーID ＋ 口座種別の組合せは一意（重複登録不可） / 非アクティブ口座への残高変動は適用しない / 銀行名・証券会社名は所有者本人のみ変更可 / 同一の引落確定通知による SMBC 残高への反映は一度だけ（冪等性：反映済み引落確定通知IDの集合で重複判定。#388） / 同一の取引による別銀行貯蓄（シャドウ）残高への反映は一度だけ（冪等性：反映済み取引IDの集合で重複判定。#390） / 手入力（取り崩し・残高補正・NISA積立累計の補正）は 1 件ごとに操作記録として口座に積む（追記のみ。#459 / #458） / 保存は版数照合で行う（読み出したときの版と一致するときだけ書き込み、不一致は拒否して後勝ちを防ぐ。#459） | 共通口座属性（口座ID・所有者ユーザーID・口座種別・登録日時・アクティブ状態・版数〔#459〕）、SMBC銀行口座（SMBC残高に反映済み引落確定通知IDの集合を内包・#388）・三井住友カード・別銀行貯蓄口座（銀行名・手入力操作記録内包・Phase 3.5 / #459）・NISA口座（証券会社名・手入力操作記録内包・Phase 3.5 / #458） の OR（各残高内包） | ユーザーID、三井住友カード未払金集約ID（参照のみ） |
| 10 | 残高・資産推移管理 | 三井住友カード未払金集約 | カード口座 1 件につき 1 件を口座登録と対で開設する（#395。作成順は口座 → 未払金集約。未払金集約が口座を参照するため。対の作成が中断したら同じ登録のやり直しで開設し直す） / 当月未払金合計 = Σ 計上中エントリ金額（集約内で整合） / 同一の引落確定通知による消込は一度だけ適用される（冪等性：引落確定通知ID で重複判定）。**残高側への反映が一度だけであることは口座集約 #9 が担保する**（#388: 消込済みでも残高が未反映なら再実行で反映まで進めるため、消込の冪等性だけでは足りない） | 三井住友カード未払金集約ID・口座ID・当月未払金合計、List<未払金エントリ>（計上中エントリ／引落消込済みエントリ OR）、最終消込日時 | 口座ID（口座集約への参照）、取引ID（各エントリ内）、引落確定通知ID |
| 10b | 残高・資産推移管理 | 銀行入金 | 取引ID で一意（同一入金の二重取込不可。Repository の一意制約が最終保証） / 用途は「用途不明 → 確定」の単方向のみ（確定済みの用途は変更不可。**同じ用途での再確定は冪等**に成功し、シャドウ残高反映・経費精算入金到着イベントの前方回復の入口になる） / 用途を確定できるのは帰属ユーザー本人のみ（給与・経費精算入金は個人に閉じる情報。#390） / 経費精算入金判別は経費精算入金ID を必ず伴う（判別時に採番して保持し、イベント再発行でも同じ ID を使う） / 入金金額は正 | 共通属性（銀行入金ID・口座ID・取引ID・帰属ユーザーID・金額・発生日時・振込元名・判別日時）、給与判別／経費精算入金判別（経費精算入金ID 内包）／別銀行戻し判別／用途不明（暫定処理 = 手動確認待ち） の OR、確定経路（自動判別 or 手動確認） | 口座ID（口座集約への参照）、取引ID、ユーザーID、経費精算入金ID（経費精算への参照） |
| 10c | 残高・資産推移管理 | 残高変動履歴エントリ | 追記のみ（一度記録した変動は書き換えない。過去のグラフが後から変わらない） / 同一の 残高軸 ＋ 由来イベントID は 1 件だけ（冪等性：イベント配信は at-least-once のため、同じ変動が二度届いても点が重ならない） | 履歴エントリID・残高軸（SMBC銀行残高／別銀行貯蓄残高／NISA積立累計／カード未払い合計）・口座ID・変動後の値・発生日時・由来イベントID | 口座ID（口座集約への参照）、由来イベントID（ドメインイベントへの参照） |
| 10d | 残高・資産推移管理 | 勤務先振込元名簿 | 名簿は利用者ごとに 1 つ（世帯共通にしない。夫婦の勤務先が別々でも各自の給与を判別できるようにする。#448 / OQ-61 ②） / 登録できるのは名簿の所有者本人のみ（勤務先の振込元名は相手に見えない。給与・経費精算入金と同じく個人に閉じる） / 登録できるのは給与判別・経費精算入金判別として確定した入金の振込元だけ（別銀行戻しの振込元を勤務先にすると自分の口座間の資金移動が給与として判別される） / 正規化済み振込元名で一意（同じ勤務先を二重に持たない。同名の再登録は冪等で、登録日時・表示用の名前を据え置く） / 追記のみ（登録を取り消す振る舞いは持たない） | 利用者ID、List<勤務先振込元名登録（正規化済み振込元名・表示用振込元名・登録日時・由来取引ID）> | ユーザーID、取引ID（登録のもとになった入金） |
| 11 | 経費精算 | 月次経費サイクル | 経費種別累計 ≤ 上限（超過分は按分子取引に分割） / 集積中 → CSV確定 → 最終確定 の単方向遷移のみ許容 / 月次経費サイクルは ユーザーID ＋ 対象年月 で一意 | 月次経費サイクル共通属性（月次経費サイクルID・ユーザーID・対象年月・サイクル開始日時）、List<経費種別累計>（上限あり OR 無制限）、List<按分子取引参照>、集積中／CSV確定／最終確定 の状態 OR | 取引ID（経費取引参照）、経費種別ID、経費精算入金ID |
| 12 | 経費精算 | 按分子取引 | 個人充当金額 > 0 でなければ生成されない / 親取引IDが必ず存在する（孤立した按分子取引は不可） / 按分根拠が必ず設定される | 子取引ID・親取引ID・ユーザーID・個人充当金額・個人費用区分・派生日時・按分根拠（上限超過先着按分 OR 経費修正再按分 OR 経費削除再按分） | 親取引ID（家計分析の取引集約への参照）、月次経費サイクルID |
| 13 | 経費精算 | 経費精算入金 | ユーザーID ＋ 入金日時で実質一意（同月の経費精算入金は 1 件のみ突合対象） / 突合待ち → 突合済み → 不認定分確定済み の単方向遷移のみ許容 | 経費精算入金ID・ユーザーID・入金金額・入金日時・突合状態（突合待ち OR 突合済み OR 不認定分確定済み） | ユーザーID、突合対象月次経費サイクルID |
| 14 | オンボーディング・認証 | アプリユーザー | LINE_userID で一意（重複登録不可） / Phase 遷移は前進のみ（Phase1完了 → Phase2進行中 → Phase2完了 → 運用開始済み） / SectionA 完了後でなければ SectionB に進めない（順序強制）。SectionB 完了後でなければ SectionC/D/E を確認・編集できない（論点8） / ニックネームは本人のみ変更可（Phase 3.5） | アプリユーザー共通属性（ユーザーID・LINE_userID・役割（Honey or Darling）・ニックネーム?（Phase 3.5）・初回登録日時）、Phase1完了～運用開始済み の状態 OR、Phase2進捗（SectionA～F進捗）、Gmail_OAuth_トークン参照、初期残高登録参照、LINE_運用設定（運用開始前は共通属性へ optional で事前蓄積し、運用開始発火で昇格・一本化 #41。改訂 2026-07-24 / #73・OQ-55: 共通トークルーム参加状態は per-user から分離し世帯レベルの置き場で保持する。改訂 2026-07-25 / #334: 通知機能有効化状態も共通トークルームID を持たず有効化日時のみ） | Gmail_OAuth_トークン保管参照ID（Parameter Store path）、初期残高登録参照（残高・資産推移管理への参照） |
| 14b | オンボーディング・認証 | 共通トークルーム | 世帯にひとつの記録のみ（シングルトン。参加は世帯にひとつの事実で join Webhook は userID を含まない） / 参加済みなら共通トークルームIDと join_Webhook受信日時が必ず揃う / 同一トークルームの再記録は受信日時を上書きしない（冪等） | 共通トークルーム参加状態（未参加 OR 参加済み（共通トークルームID・join_Webhook受信日時）） | 共通トークルームID（LINE 外部由来） |
| 14c | オンボーディング・認証 | 世帯通知有効化記録 | 世帯にひとつの記録のみ（シングルトン。有効化は世帯にひとつの事実） / 有効化日時は記録後に書き換えない（冪等。配信の冪等性キーの一部であり、変わると同じテストメッセージが再び届く） / 記録は通知配信からの `TestMessageSent`（LINE への配信確定）を検知して初めて書く（発行そのものの失敗・配信の `retry_abandoned` はいずれも記録が残らずやり直せる、#447 / #590） | 世帯通知有効化状態（世帯未有効化 OR 世帯有効化済み（有効化日時）） | なし |
| 14d | オンボーディング・認証 | Gmail_OAuth_トークン | ユーザーごとに 1 件（UserId キー、専用 ID なし） / 有効 → 失効検知済み → （再認可で）有効 の状態遷移のみ / トークン実体は Parameter Store に保管し、本集約は保管参照（パス）のみ保持 / 失効時の通知は個別 DM のみ（OQ-2 ✅: メールフェイルセーフ対象外） | 有効（ユーザーID・トークン保管参照・認可日時・最終確認日時） OR 失効検知済み（ユーザーID・トークン保管参照・認可日時・失効検知日時・失効理由（API呼び出し失敗 OR 期限切れ）） | ユーザーID、トークン保管参照（Parameter Store path） |
| 14e | オンボーディング・認証 | アクセス拒否カウンタ | LINE_userID で一意（相手ごとに 1 件へ集約。個々の発生時刻の履歴は残さない、決定 A-1） / 拒否累計回数は正の整数（0件のカウンタは存在しない。1件目の拒否で初めて作られる） | LINE_userID・拒否累計回数・最終拒否日時 | なし（LINE_userID は本集約自身のキー） |
| 15 | 通知配信 | 配信メッセージ | 配信先が確定済みでなければ送信処理に進まない / 送信成功・送信スキップは最終状態（再送信不可） / 配信用途×配信先の整合（用途ごとに要求される配信先種別が決まる: CSV取込リマインダー・世帯サマリ・テストメッセージ = 共通トークルーム、本人サマリ・OAuth失効通知 = 個人DM） | 配信メッセージ共通属性（配信メッセージID・配信先・配信内容・配信用途）、送信予約済み～送信スキップ の状態 OR、送信失敗時のリトライ状態 | 共通トークルームID、ユーザーID（LINE_userID） |
| 16 | 通知配信 | LINE配信ログ | 冪等性キーで同月レポート再送信の重複を防ぐ（同一冪等性キーで配信を確定させるログは 1 件のみ） / 未達が確定した送信失敗（LINE_API_失敗）のログだけは配信を確定させず、同一冪等性キーで再送信できる / 送信完了後のログは変更不可（監査記録） | 配信ログID・配信メッセージID・配信タイミング種別・配信先・送信payload_json・送信結果ステータス・送信日時・冪等性キー | 配信メッセージID、月次レポートID、取込ジョブID |
| 17 | 通知配信 | フェイルセーフメール | しきい値到達済みの連続失敗カウンタがある場合のみ生成される / 送信成功は最終状態（再送信不可） | フェイルセーフメール共通属性（フェイルセーフメールID・宛先メールアドレス・件名・メール本文・起因連続失敗カウンタ参照）、送信予約済み～送信失敗 の状態 OR | ユーザーID（または 共通トークルームID）（起因連続失敗カウンタ参照として） |
| 18 | マスタ管理 | カテゴリマスタ | 同一スコープ（世帯共有または個人別）内でカテゴリ名は一意 / 規定カテゴリは削除・改名不可 / 規定カテゴリは世帯共有スコープ、追加カテゴリは個人別スコープ（superRefine） / 削除時は移動先カテゴリIDが必ず設定される（孤立取引を作らない） | カテゴリマスタ共通属性（カテゴリID・カテゴリ名・所有スコープ）、規定カテゴリ（種別固定）OR 追加カテゴリ（作成日時・作成者・改名履歴） | 削除リクエスト内の移動先カテゴリID（自コンテキスト内参照） |
| 19 | マスタ管理 | 経費種別マスタ | 同一スコープ内で経費種別名は一意 / 規定経費種別は削除・改名不可 / 規定経費種別は世帯共有スコープ、追加経費種別は個人別スコープ（superRefine） / 削除時は移動先経費種別IDが必ず設定される | 経費種別マスタ共通属性（経費種別ID・経費種別名・所有スコープ）、規定経費種別 OR 追加経費種別（作成日時・作成者・改名履歴） | 削除リクエスト内の移動先経費種別ID（自コンテキスト内参照） |
| 20 | マスタ管理 | 月次上限 | ユーザーID ＋ 経費種別ID で一意（重複する上限設定不可） / 「上限あり月次上限」と「無制限月次上限」が同時に存在しない（OR で完全分離） / 上限金額を持ちながら無制限状態は不可 | 月次上限共通属性（月次上限ID・ユーザーID・経費種別ID・適用開始日時）、上限あり月次上限（上限金額・変更履歴）OR 無制限月次上限 | ユーザーID、経費種別ID（経費種別マスタへの参照） |
| 21 | マスタ管理 | Phase0設定値 | LINE_Channel設定・LINE_Webhook設定・許可リスト設定が揃って初めて運用可能（三要素の整合性） / 設定値は Parameter Store（KMS）に保管され、本集約はパスのみ保持 | LINE_Channel設定（Channel_ID保管参照・Channel_Secret保管参照・Channel_Access_Token保管参照・投入日時・LINE公式アカウント識別）、LINE_Webhook設定（Webhook_URL・Lambda紐付け参照・設定日時）、許可リスト設定（夫・妻LINE_userID保管参照・投入日時） | AWS Parameter Store（外部システム参照、ID = Parameter_Store_path） |

---

## 2. 集約境界の議論

### 2.1 集約サイズの妥当性

**各集約のサイズ評価:**

| 集約ルート | サイズ評価 | 理由 |
|---|---|---|
| 取引候補 | 適切 | 1 取引 1 集約。状態変化（通常 / Amazon突合 / タイムアウト未分類 / 確定済み）が単一トランザクションで管理される |
| 日次メール取込バッチ | 適切 | バッチ単位でライフサイクルを管理。バッチ内の取引候補は別集約（取引候補）として独立 |
| 明細取込ジョブ | 適切 | 1 アップロード 1 ジョブ。状態遷移が単方向で管理可能なサイズ |
| 加盟店学習ルール | 適切 | ユーザー ＋ 加盟店名の 1 ルール 1 集約。T-2 フィールド独立の不変条件を 1 集約で完結 |
| 一括分類セッション | 適切 | CSV 取込 1 件につき 1 セッション。ただし List<未分類取引> が大量になる可能性あり → 件数が多い場合はページング（結果整合）で対処するのが現実的だが、不変条件（セッション単位の管理）は集約内で完結するため許容範囲 |
| 取引（家計分析） | 適切 | 1 取引 1 集約。分類状態の OR 表現で不変条件が集約内に閉じている |
| 月次レポート | やや大きい（許容） | 複数費用区分の合計・NISA・残高推移を 1 集約に内包。しかし「月次レポートの CSV確定 / 最終確定」という状態遷移の整合性は 1 集約で守るべき範囲であり、分割すると不変条件が崩れる。許容する |
| 口座（残高） | 適切 | 1 口座 1 集約。三井住友カードは未払金集約への参照（ID）のみを持ち、実体を内包しない設計が適切 |
| 三井住友カード未払金集約 | 適切 | 「当月未払金合計 = Σ エントリ」という不変条件を 1 集約で守る。エントリ数は月内カード利用件数に限定されるため過大にならない |
| 月次経費サイクル | 適切 | 月次・ユーザー単位の 1 集約。経費種別累計リストと按分子取引参照リストを内包するが、「累計 ≤ 上限」という不変条件の整合性はこの範囲で完結する |
| 按分子取引 | 適切 | 上限超過時の 1 件の派生取引 1 集約。親取引IDを保持するが親集約の実体を参照しない（ID のみ） |
| 経費精算入金 | 適切 | 1 入金 1 集約。突合状態の遷移が集約内に閉じている |
| アプリユーザー | 適切 | 1 ユーザー 1 集約。Phase 進捗を内包するが、Phase ごとの順序強制（SectionA → SectionB）が 1 集約で守るべき不変条件のため適切 |
| 配信メッセージ | 適切 | 1 メッセージ 1 集約。送信ライフサイクル（予約 → 送信 → 成功 / 失敗 / スキップ）が集約内に閉じている |
| LINE配信ログ | 適切 | 1 送信記録 1 集約。冪等性キーで重複を防ぐ（未達が確定した失敗は配信を確定させないため、同一キーに失敗の履歴が並ぶ）。変更不可の監査記録として扱う |
| フェイルセーフメール | 適切 | 1 メール 1 集約。送信ライフサイクルが集約内に閉じている |
| カテゴリマスタ | 適切 | 1 カテゴリ 1 集約。改名履歴を内包するが、カテゴリ単位の管理に閉じている |
| 経費種別マスタ | 適切 | カテゴリマスタと同構造。1 経費種別 1 集約 |
| 月次上限 | 適切 | ユーザー ＋ 経費種別の 1 設定 1 集約。「上限あり OR 無制限」の状態が 1 集約で管理される |
| Phase0設定値 | 許容（やや特殊） | LINE_Channel設定・LINE_Webhook設定・許可リスト設定という 3 要素を 1 集約に内包。「三要素が揃って初めて運用可能」という不変条件を守るためにまとめることは正当。ただし将来的に各要素が独立して更新されるケースが増えた場合は分割を検討する |

**結論:** 21 集約のサイズはいずれも妥当。月次レポートと Phase0設定値はやや大きい傾向があるが、不変条件の整合性を守るために必要な範囲として許容する。

---

### 2.2 集約間参照は ID か

全集約の参照方式を検査した。以下の点について確認:

**OK（ID参照で完結）:**

| 参照元集約 | 参照先集約 | 参照方式 |
|---|---|---|
| 口座（三井住友カード）→ 三井住友カード未払金集約 | `三井住友カード未払金集約ID`（ID のみ）| OK |
| 取引（家計分析）→ カテゴリマスタ | `カテゴリID` | OK |
| 取引（家計分析）→ 経費種別マスタ | `経費種別ID` | OK |
| 月次レポート → 経費精算入金 | `経費精算入金ID` | OK |
| 按分子取引 → 取引（家計分析） | `親取引ID`（= 取引ID） | OK |
| 経費精算入金 → 月次経費サイクル | `突合対象月次経費サイクルID` | OK |
| 月次経費サイクル → 取引（家計分析） | `取引ID`（経費取引参照・按分子取引参照内） | OK |
| 加盟店学習ルール → カテゴリマスタ | `カテゴリID` | OK |
| 加盟店学習ルール → 経費種別マスタ | `経費種別ID` | OK |
| 月次上限 → 経費種別マスタ | `経費種別ID` | OK |
| アプリユーザー → 初期残高（残高・資産推移管理） | `初期残高登録参照`（SMBC初期残高ID 等の ID 群） | OK |
| 通知配信系集約 → オンボーディング・認証 | `ユーザーID`、`共通トークルームID` | OK |
| 取引候補（確定済み）→ 取引（家計分析） | `取引ID`（生成された取引への参照。取引候補側は取引の実体を内包しない） | OK |

**要確認点（結論: 問題なし）:**

- `08d-ul-残高資産推移管理.md` の `三井住友カード` data 内に `三井住友カード未払金集約参照 = 三井住友カード未払金集約ID` と定義されており、参照は ID 経由。実体の内包はない。

**結論:** 全集約間参照が ID 経由になっており、直参照は存在しない。修正不要。

---

### 2.3 不変条件の閉包性

各集約の不変条件が、その集約内のデータだけで判定できるかを確認する。

**完全に閉包している（集約内データのみで判定可能）:**

| 集約ルート | 不変条件 | 閉包性評価 |
|---|---|---|
| 取引候補 | Gmail_message_ID 重複なし | 集約内で Gmail_message_ID を保持。重複判定は `behavior メールの重複を判定する` で行い、結果（重複判定結果）を受けて取引候補を生成する設計 → OK |
| 日次メール取込バッチ | 二重起動なし | バッチID・ユーザーID・バッチ起動日時を内包。二重起動の防止はバッチ起動 behavior の事前条件で行うが、バッチの状態（起動済み・取込中）は集約内データで判定可能 → OK |
| 加盟店学習ルール | F-1 ユーザーID ＋ 加盟店名で一意 | 一意性は集約を生成する behavior（`手動修正を学習に反映する`）でリポジトリ検索により保証する想定。集約内には ユーザーID と 加盟店名 の両方を保持しており、生成時の一意性チェックに必要な情報が揃っている → OK |
| 取引（家計分析） | 経費(会社) 取引は 経費種別ID 必須 | `data 経費種別参照 = 経費(会社)取引 OR 非経費取引` / `data 経費(会社)取引 = 経費種別ID` と定義済み。経費種別参照の型選択により集約内で不変条件を保証 → OK |
| 月次レポート | CSV確定 → 最終確定 の単方向遷移 | 現在の状態（CSV確定月次レポート OR 最終確定月次レポート）を集約内で保持し、遷移は behavior で制御 → OK |
| 月次経費サイクル | 経費種別累計 ≤ 上限 | `List<経費種別累計>` と `月次上限`（経費精算コンテキスト内で保持）を参照する必要がある。**月次上限の実値（上限金額）はマスタ管理の月次上限集約に存在**するため、`行動 経費種別累計を上限と照合する` では月次上限の値を参照している |
| 按分子取引 | 個人充当金額 > 0 / 親取引IDが必ず存在する | 按分子取引の生成 behavior に事前条件として明示。生成後は集約内に 親取引ID と 個人充当金額 を内包 → OK |
| 月次上限 | 「上限あり OR 無制限」の排他性 | `data 月次上限 = 上限あり月次上限 OR 無制限月次上限` で型レベルで保証 → OK |

**結果整合性で対処すべき不変条件:**

| 集約ルート | 他の集約に依存する不変条件 | 対処方法 |
|---|---|---|
| 月次経費サイクル（経費種別累計） | 「経費種別累計 ≤ 上限」の上限値はマスタ管理の月次上限集約が保有。月次上限変更イベント発生時に経費精算コンテキストで再計算が必要 | `behavior 月次上限を変更する`（マスタ管理）の出力イベントを受けて `behavior 取引修正で按分を自動再計算する`（経費精算）がトリガーされる設計。**イベント駆動での結果整合性**として対処する。08h-ul-マスタ管理.md の `behavior 月次上限を変更する` の事後条件に「経費精算へ変更を通知し、当月の按分が再計算される」と記載済みのため、設計として織り込み済み → OK（修正不要） |
| 取引（家計分析） | 取引削除時に残高・資産推移管理の口座残高が再計算される必要がある | `behavior 取引を削除する` の事後条件に「残高・資産推移管理に削除を通知し、残高・未払金が再計算される」と記載済み。取引削除イベントを受けた残高・資産推移管理が結果整合で対処 → OK（修正不要） |
| 三井住友カード未払金集約 | 引落消込の冪等性を保証するために引落確定通知IDが必要だが、通知ID は取引取込コンテキストが生成 | 引落確定通知IDを `計上中エントリ` → `引落消込済みエントリ` 遷移の際に保持することで、同一引落確定通知IDの再適用を集約内で検知可能 → OK |

**結論:** 全集約の不変条件は集約内データで判定可能か、またはイベント駆動での結果整合性として設計に織り込み済み。修正不要。

---

### 2.4 セルフレビュー結果

- [x] §1 表に 21 個の集約ルートが網羅されている（grep 結果と一致）
- [x] 各集約ルートに不変条件が 1〜3 個書かれている
- [x] 集約内 data が列挙されている（主要な値オブジェクト・子エンティティを記述）
- [x] 外部参照が ID 型のみであることを確認（直参照なし）
- [x] §2.1 / §2.2 / §2.3 が埋まっている
- [x] 08*-ul-*.md への修正なし（既存ファイルの設計で問題なし）
