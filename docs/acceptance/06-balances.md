# 06. 残高・資産推移(AT-5xx)

> ⚠ 残高の手動操作(取り崩し・補正・初期残高の後修正・口座非アクティブ化)の API は #397 で実装済み(`/api/accounts/:id` 配下)だが、これを呼ぶ画面(口座詳細 #406)が未実装のため、画面からの書き込みは初期残高入力(AT-102)経由に限られる。手動操作の受入は画面の実装後に AT-907(90-pending.md)から昇格させる。メール由来の残高自動更新は未実装。

---

## AT-501: 残高一覧と世帯合計

- 目的: 口座別残高と世帯合計が正しく表示されること
- 対象: `${WEB_URL}/balances`
- 実施者: 両方
- 前提条件: AT-102(初期残高入力)完了

| # | 操作 | 期待結果 | ☐ |
| --- | --- | --- | --- |
| 1 | 残高画面を開く | SMBC 残高 / 別銀行貯蓄 / NISA 累計 / カード未払金が口座別に表示される | ☐ |
| 2 | 各金額を入力値と突き合わせる | AT-102 で入力した値と一致する | ☐ |
| 3 | 世帯合計を確認 | 可視範囲ルール(AT-404)に基づく合計値が正しい | ☐ |

- 事後処理: なし(読み取りのみ)
- 備考: 依存 API `GET /api/balances`, `GET /api/balances/total`

### 実施記録

| 実施日 | 環境 | コミット/タグ | 実施者 | 結果 | 備考 |
| --- | --- | --- | --- | --- | --- |
| | | | | | |

---

## AT-502: 残高の初期値反映

- 目的: 初期残高の登録・修正が残高表示に反映されること(P2-B4: 初期残高は後から修正可能)
- 対象: `${WEB_URL}/onboarding`(Section B), `${WEB_URL}/balances`
- 実施者: 両方
- 前提条件: AT-102 完了

| # | 操作 | 期待結果 | ☐ |
| --- | --- | --- | --- |
| 1 | 初期残高の修正画面を開き、NISA 累計を +1 円した値で保存 | 保存成功 | ☐ |
| 2 | 残高画面を再表示 | 修正後の値が反映されている | ☐ |
| 3 | 元の正しい値に戻して保存 | 元の値に戻る | ☐ |

- 事後処理: 手順 3 で原状回復済みであることを確認
- 備考: ⚠ API は `PUT /api/accounts/:id/initial-balance`(#397)で実装済み。呼び出す画面(#406)が未接続のうちは「未接続」と記録し、実装後に再実施(AT-102 の備考と同様)

### 実施記録

| 実施日 | 環境 | コミット/タグ | 実施者 | 結果 | 備考 |
| --- | --- | --- | --- | --- | --- |
| | | | | | |

---

## AT-503: 残高推移グラフ

- 目的: 残高推移(time-series)が時系列グラフとして表示されること
- 対象: `${WEB_URL}/balances`
- 実施者: どちらか
- 前提条件: 残高データが 1 時点以上あること

| # | 操作 | 期待結果 | ☐ |
| --- | --- | --- | --- |
| 1 | 残高画面の推移グラフを表示 | 時系列チャートが描画される(データ 1 点でもエラーにならない) | ☐ |
| 2 | 期間・粒度の切り替え(あれば)を操作 | 表示が切り替わりエラーが出ない | ☐ |
| 3 | AT-502 の修正後に再表示 | 推移に修正が反映される | ☐ |

- 事後処理: なし
- 備考: 依存 API `GET /api/balances/time-series`。日々の自動残高更新(メール由来)は未稼働のためデータ点は少ない前提で確認する

### 実施記録

| 実施日 | 環境 | コミット/タグ | 実施者 | 結果 | 備考 |
| --- | --- | --- | --- | --- | --- |
| | | | | | |
