# Phase 3.5 クローズアウト + Phase 4 引き継ぎ準備 実行計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Phase 3.5 spec で確定した UX/UI 設計を、後続フェーズが安心して参照できる状態にクローズアウトし、Phase 4 戦術的設計の writing-plans 起動準備を完了させる。

**Architecture:** 設計トークン抽出 → ワイヤーフレームインデックス作成 → spec/DDD 整合性検証 → 残課題の §B 追記 → Phase 4 引き継ぎサマリ作成 → DoD 検証 の 6 タスク構成。すべてドキュメント作業、コード実装はゼロ。

**Tech Stack:** Markdown のみ。drawio/HTML モックは既存（`.superpowers/brainstorm/`、gitignore 済）を参照。

**Spec:** [docs/superpowers/specs/2026-05-01-phase3.5-ux-ui-design.md](../specs/2026-05-01-phase3.5-ux-ui-design.md)

**前提読書（タスク開始前に必ず読む）:**
- 上記 spec 全文（特に §2 ビジュアル規約 / §3 ロール名 / §14 ドメインへの影響 / §15 未確定事項 / §16 DoD）
- `docs/domain/01-overview.md` §6.6 画面構成
- `docs/domain/07-bounded-contexts.md` §1（Phase 3.5 反映済み）
- `docs/domain/08d-ul-残高資産推移管理.md` ヘッダ + data 別銀行貯蓄口座 / NISA口座
- `docs/domain/08f-ul-オンボーディング認証.md` ヘッダ + data 役割 / アプリユーザー共通属性
- `docs/domain/09-aggregates.md` #9 / #14
- `docs/domain/03-open-questions.md` §A.5 と §B

---

## File Structure

```
docs/
├─ domain/
│   ├─ 03-open-questions.md          (modify §B に Phase 3.5 残課題を追記)
│   └─ wireframes/                   (create, 新規ディレクトリ)
│       ├─ README.md                 (create, 12 画面のインデックスと spec アンカー)
│       └─ design-tokens.md          (create, 色・タイポ・余白の参照値)
│
└─ superpowers/
    ├─ plans/
    │   └─ 2026-05-01-phase3.5-closeout.md   (本ファイル)
    │
    └─ specs/
        └─ 2026-05-01-phase3.5-ux-ui-design.md
            (modify, §16 DoD のチェック反映 / §15 残課題のステータス更新)
```

`.superpowers/brainstorm/68872-1777617623/content/*.html` は gitignore 対象だが Visual Companion のセッション保管として残す（永続的な参照は wireframes/README.md でリンクしない=コピー必須箇所のみ HTML を保存）。

---

## Task 1: design-tokens.md を作成

**Files:**
- Create: `docs/domain/wireframes/design-tokens.md`

**Goal:** spec §2 ビジュアル規約 から「色」「タイポ」「角丸」「シャドウ」を抽出し、Phase 4 以降で参照できるようカタログ化する。

- [ ] **Step 1.1: ディレクトリ作成**

```bash
mkdir -p docs/domain/wireframes
```

- [ ] **Step 1.2: design-tokens.md を新規作成**

ファイル内容を以下のとおり書く（Markdown、コードブロックは CSS 形式の値を使用）:

````markdown
# デザイントークン

> 親: [Phase 3.5 spec §2](../../superpowers/specs/2026-05-01-phase3.5-ux-ui-design.md)
> 用途: Phase 4 戦術的設計（TS 型 + Zod）以降の UI 実装で参照する色・タイポ・余白の正本。

## 1. テーマカラー（per-user）

### 1.1 Darling（妻）テーマ

| トークン | 値 | 用途 |
|---|---|---|
| `--bg-gradient` | `linear-gradient(180deg, #ffc8d8 0%, #ffd8e0 30%, #ffe0e8 60%, #fff0f5 100%)` | パネル背景 |
| `--kpi-1` | `linear-gradient(135deg, #ff8aa8, #ffb0c5)` | KPI カード（標準） |
| `--kpi-hero` | `linear-gradient(135deg, #ee5d8a, #ff80a8)` | 資産合計カード |
| `--accent-rose` | `#ff7095` | アクセント |
| `--text-primary` | `#5a2840` | 主テキスト |
| `--text-secondary` | `#a04068` | 補助テキスト |
| `--text-on-kpi` | `#ffffff` | KPI カード上の数字 |

### 1.2 Honey（夫）テーマ

| トークン | 値 | 用途 |
|---|---|---|
| `--bg-gradient` | `linear-gradient(180deg, #98c0e0 0%, #b8d0e8 30%, #d0e0f0 60%, #e8f0f8 100%)` | パネル背景 |
| `--kpi-1` | `linear-gradient(135deg, #5888c0, #80a8d0)` | KPI カード（標準） |
| `--kpi-hero` | `linear-gradient(135deg, #2a5098, #4878b8)` | 資産合計カード |
| `--accent-royal` | `#5090d8` | アクセント |
| `--text-primary` | `#1a3868` | 主テキスト |
| `--text-secondary` | `#2a5098` | 補助テキスト |
| `--text-on-kpi` | `#ffffff` | KPI カード上の数字 |

## 2. カテゴリ色（テーマで色相のみ調整）

| カテゴリ | Darling | Honey |
|---|---|---|
| 住居光熱通信 | `#d8407a` | `#1a4080` |
| 食費 | `#ff6f90` | `#3878c8` |
| 娯楽 | `#ff9888` | `#48a8c0` |
| その他 | `#c870b0` | `#7080c0` |

## 3. アラート色（テーマ非依存）

| 用途 | 値 |
|---|---|
| 警告（黄） | `#fff5e0` 背景 / `#f0c878` 枠 / `#a05028` 文字 |
| 危険（赤） | `#ffe0e0` 背景 / `#f08080` 枠 / `#a02828` 文字 |
| 成功（緑） | `#dcf0e0` 背景 / `#9ace6a` 枠 / `#3a8068` 文字 |
| 通知バッジ | `#9ace6a` 背景 / `#fff` 文字（CSV確定） |

## 4. タイポグラフィ

```css
font-family: 'Hiragino Maru Gothic ProN', 'Yu Gothic', system-ui, -apple-system, sans-serif;

/* 重要数字（KPI 値）*/
font-size: 14px-16px;
font-weight: 700-800;

/* hero 数字（月次レポートのメイン）*/
font-size: 24px-30px;
font-weight: 800;

/* KPI ラベル */
font-size: 9px;
opacity: 0.95;
font-weight: 600;

/* 補助情報 */
font-size: 8px-9px;
opacity: 0.55-0.85;
```

## 5. 角丸・シャドウ

| 要素 | 値 |
|---|---|
| カード（標準） | `border-radius: 14px` |
| ヒーローカード | `border-radius: 14px` + `border: 2px solid` |
| パネル全体 | `border-radius: 20px` |
| ボタン | `border-radius: 9-12px` |
| ピル（バッジ） | `border-radius: 12-14px` |
| シャドウ標準 | `box-shadow: 0 2px 8px <theme>20` |
| シャドウ強 | `box-shadow: 0 4px 14px <theme>50` |

## 6. 余白

| 用途 | 値 |
|---|---|
| カード内 padding | `11px-12px` |
| カード間 gap | `6px-10px` |
| パネル padding | `14px` |
| 画面幅 | `300px`（LIFF スマホ縦想定） |

## 7. 装飾要素

- 背景に opacity 0.4 で散らす絵文字（Darling: ✨ ♡ 🌸 / Honey: ✦）
- 重要 KPI（資産合計）に隅装飾（✨）
- ストロークは丸キャップ（`stroke-linecap="round"`）でドーナツチャートをキュート化

## 8. 役割別アバター絵文字

| ロール | 絵文字 | 表記例 |
|---|---|---|
| Honey（夫） | ⛵ | 「⛵ コウキ」 |
| Darling（妻） | 🌸 | 「🌸 ななみ」 |
````

- [ ] **Step 1.3: ファイル作成を確認 + コミット**

```bash
ls docs/domain/wireframes/design-tokens.md
git add docs/domain/wireframes/design-tokens.md
git commit -m "docs(domain): Phase 3.5 デザイントークンを wireframes/ に追加

色・タイポ・余白・装飾の参照値を Phase 4 以降が引ける形で記録。
spec §2 ビジュアル規約からの抽出。"
```

---

## Task 2: ワイヤーフレームインデックス README を作成

**Files:**
- Create: `docs/domain/wireframes/README.md`

**Goal:** Visual Companion で生成した HTML モックの保管場所と、各画面の設計意図 + spec の該当セクションを 1 枚のインデックスにまとめる。

- [ ] **Step 2.1: README.md を新規作成**

ファイル内容（Markdown）:

```markdown
# ワイヤーフレームインデックス

> 親: [Phase 3.5 spec](../../superpowers/specs/2026-05-01-phase3.5-ux-ui-design.md)

## 1. 保管場所

- ローカルセッション: `.superpowers/brainstorm/68872-1777617623/content/*.html`（gitignore 対象）
- 本リポジトリには直接コピーしない（HTML は Visual Companion 専用、本実装時は React/TSX で再構築）
- 確定した設計値は本ディレクトリの [`design-tokens.md`](./design-tokens.md) と spec 本体を参照

## 2. 画面 × spec マッピング

| # | 画面 | 該当 HTML | spec 該当節 |
|---|---|---|---|
| 1 | ダッシュボード（妻 v6 ピンク） | dashboard-pink-v9.html | §5 |
| 2 | ダッシュボード（夫 A3+ パステルスカイ） | dashboard-husband-a3-deeper.html | §5 |
| 3 | 世帯/個人切替プレビュー | dashboard-toggle-v1.html | §5.4 |
| 4 | ニックネーム反映プレビュー | honey-darling-v1.html | §3.3, §5 |
| 5 | ドリルダウン仕様 | drilldown-v1.html | §5.5 |
| 6 | 月次レポート画面 | monthly-report-v1.html | §6 |
| 7 | 取引一覧 + 編集モーダル A 案 | transaction-list-v1.html / transaction-modal-v2.html | §7 |
| 8 | 経費精算管理タブ | expense-tab-v1.html | §8 |
| 9 | 口座タブ + 個別詳細 | accounts-v1.html / accounts-v3-graphs.html | §9 |
| 10 | CSV/PDF 取込画面（取得 URL 案内付き） | csv-import-v3-guide.html | §10 |
| 11 | オンボーディング 4 Phase | onboarding-v1.html / honey-darling-v1.html (Phase 1) | §11 |
| 12 | LINE 通知 + Deep Link | notifications-v1.html | §12 |
| 13 | 設定（プロフィール / 口座管理） | nickname-v1.html / customization-v1.html | §13 |

## 3. 確定済み設計値（参照ハブ）

| 項目 | 参照先 |
|---|---|
| テーマカラー | [`design-tokens.md` §1](./design-tokens.md) |
| カテゴリ色 | [`design-tokens.md` §2](./design-tokens.md) |
| ロール名・ニックネーム | spec §3 |
| ナビゲーション（4 タブ） | spec §4 |
| ドリルダウン仕様（11 要素） | spec §5.5 |
| 状態バッジ（暫定/CSV確定/最終確定） | spec §6.2 |
| 取引編集モーダル A 案 | spec §7.4 |
| 時系列グラフ仕様（万単位） | spec §9.4 |
| LINE 通知 5 種 + Deep Link | spec §12 |

## 4. Phase 4 戦術設計が拾うべきポイント

- 各画面が要求する **データ契約**（どのフィールドがどの集約から来るか）→ spec §5/§6/§7
- **プライバシー 3 段階の実装位置**（ドメインサービス / リポジトリ / Read Model のいずれ）→ spec §17.1
- **集約の追加属性**（nickname / 銀行名 / 証券会社名）→ spec §14、08d / 08f / 09 で反映済
- **Deep Link URL パラメータ**（/import?month=YYYY-MM 等）→ spec §12.2
```

- [ ] **Step 2.2: ファイル作成を確認 + コミット**

```bash
ls docs/domain/wireframes/README.md
git add docs/domain/wireframes/README.md
git commit -m "docs(domain): Phase 3.5 ワイヤーフレームインデックスを追加

12 画面の HTML モック保管場所と spec の該当節をマッピング。
Phase 4 戦術設計が拾うべきポイントも記載。"
```

---

## Task 3: spec ↔ DDD docs の整合性検証

**Files:**
- Read-only: `docs/domain/07-bounded-contexts.md` / `08d-ul-残高資産推移管理.md` / `08f-ul-オンボーディング認証.md` / `09-aggregates.md` / `03-open-questions.md`
- Read-only: `docs/superpowers/specs/2026-05-01-phase3.5-ux-ui-design.md` §14.5

**Goal:** spec §14.5 のファイル別更新マッピングが、実際の DDD ファイルの内容と一致しているか目視検証。不整合があれば修正。

- [ ] **Step 3.1: spec §14.5 を読み、各エントリを記憶**

```bash
sed -n '/14.5 ファイル別更新マッピング/,/^## 15/p' docs/superpowers/specs/2026-05-01-phase3.5-ux-ui-design.md
```

11 エントリ（07/08d/08f/09/03）の内容を頭に入れる。

- [ ] **Step 3.2: 各 DDD ファイルで該当箇所を確認**

```bash
grep -n "Phase 3.5\|Honey\|Darling\|ニックネーム\|銀行名\|証券会社名" \
  docs/domain/07-bounded-contexts.md \
  docs/domain/08d-ul-残高資産推移管理.md \
  docs/domain/08f-ul-オンボーディング認証.md \
  docs/domain/09-aggregates.md \
  docs/domain/03-open-questions.md
```

期待する検出箇所:
- `07-bounded-contexts.md`: ヘッダ Phase 3.5 注記 + §1 #4 / #6 行に Phase 3.5 追加
- `08d-ul-残高資産推移管理.md`: ヘッダ + data 別銀行貯蓄口座（銀行名）/ NISA口座（証券会社名）/ 銀行名・証券会社名変更 behavior
- `08f-ul-オンボーディング認証.md`: ヘッダ + data 役割 = Honey OR Darling + ニックネーム属性 + 関連 data 名（HoneyLINE_userID 等）+ ニックネーム変更 behavior
- `09-aggregates.md`: ヘッダ Phase 3.5 注記 + #9 / #14 の不変条件・集約内 data 更新
- `03-open-questions.md`: §A.5 論点50〜57

- [ ] **Step 3.3: 不整合があれば修正、なければ次へ**

不整合の典型例:
- spec §14.5 に書いた更新箇所が DDD ファイルにない → DDD ファイル側を更新
- DDD ファイルにあるが spec §14.5 に未記載 → spec を更新
- 表記揺れ（Honey/Darling ↔ 夫/妻）→ 該当箇所のみ修正、または旧表記を残置（01-overview の歴史的経緯）

- [ ] **Step 3.4: 整合性 OK ならコミット不要、修正したらコミット**

修正があった場合のみ:

```bash
git add <修正したファイル>
git commit -m "docs(domain): Phase 3.5 spec と DDD docs の整合性修正"
```

修正なしならスキップ。

---

## Task 4: 03-open-questions.md §B に Phase 3.5 残課題を追記

**Files:**
- Modify: `docs/domain/03-open-questions.md`（§B 末尾に追記）

**Goal:** Phase 3.5 spec §15 未確定事項のうち、後続フェーズで詰める必要があるものを正式な OQ 番号付きで §B に記録する。

- [ ] **Step 4.1: §B 末尾の OQ 最大番号を確認**

```bash
grep -E "OQ-[0-9]+" docs/domain/03-open-questions.md | sort -u
```

最大の OQ 番号を確認（例: OQ-30 が最大なら、新規は OQ-31 から開始）。

- [ ] **Step 4.2: §B に OQ-31 〜 OQ-34 を追加**

`docs/domain/03-open-questions.md` の §B 末尾（| OQ-30 | ... | の次）に、以下の行を追加（OQ 番号は Step 4.1 で確認した最大+1 から連番）:

```markdown
| OQ-31 | アプリ名の確定 | Phase 3.5 で候補（ふたりばこ / ふたり家計 / おうち家計 / わたしたちの家計 / Coinic / イエカケイ）を提示済み。Phase 4 開始までに確定する。決定後、本リポジトリ内のすべての「（仮）」表記を一括置換 |
| OQ-32 | 三井住友銀行（SMBC ダイレクト）の月別明細 URL パターン | Phase 3.5 spec §10.2 で URL テンプレートを設計したが、実 URL は未確認。Phase 4 で SMBC ダイレクトに実ログインして月別明細ページのブックマーク URL を取得し、可能なら `?month=YYYY-MM` 等のクエリで月指定できるか確認。不可なら「ログイン後に該当月を探す」手順に切替 |
| OQ-33 | LINE Flex Message のサイズ制限と各人別リンク同梱 | Phase 3.5 spec §12 で CSV取込リマインダーに「Honey 用 / Darling 用」リンク 2 セット計 4 個の同梱を計画。LINE Messaging API の Flex Message サイズ制限（一般に 50KB / コンポーネント数制限）に収まるかを Phase 4 のサンプルメッセージで検証 |
| OQ-34 | テーマカラーのユーザー切替対応 | Phase 3.5 spec §13.1 で設定画面に placeholder のみ。実装するか・複数テーマを提供するかは Phase 4〜6 で判断。最低限「Darling 標準ピンク / Honey 標準ネイビー」の 2 種固定で運用開始する案を仮置き |
```

- [ ] **Step 4.3: 追加箇所を確認 + コミット**

```bash
grep -E "OQ-3[1-4]" docs/domain/03-open-questions.md
git add docs/domain/03-open-questions.md
git commit -m "docs(domain): Phase 3.5 残課題を §B に追記 (OQ-31〜34)

アプリ名・三井住友銀行 URL・Flex Message サイズ・テーマ切替の
4 件を後続フェーズで詰める論点として記録。"
```

---

## Task 5: Phase 4 引き継ぎサマリ作成

**Files:**
- Create: `docs/superpowers/plans/2026-05-01-phase4-handoff.md`

**Goal:** Phase 4 戦術的設計（次フェーズ）が `writing-plans` を起動するときに参照する「Phase 3.5 から引き継ぐべき決定事項」を 1 枚に整理する。

- [ ] **Step 5.1: ファイルを新規作成**

ファイル内容:

```markdown
# Phase 4 戦術的設計 引き継ぎサマリ

> 作成: 2026-05-01
> 親 spec: [Phase 3.5 UX/UI 設計](../specs/2026-05-01-phase3.5-ux-ui-design.md)
> 用途: Phase 4 ブレストおよび writing-plans 起動時に「Phase 3.5 から決まっている前提」を一望

## 1. ドメイン側の確定変更（DDD docs に反映済み）

| 集約 | 追加 / 変更 | 反映先 |
|---|---|---|
| アプリユーザー（オンボーディング・認証） | `nickname?` 属性追加、役割 = `Honey \| Darling` | 08f / 09 #14 |
| 別銀行貯蓄口座（残高・資産推移管理） | `銀行名` 属性追加 | 08d / 09 #9 |
| NISA 口座（残高・資産推移管理） | `証券会社名` 属性追加 | 08d / 09 #9 |

Phase 4 の TS 型化で **これらを前提にスキーマを書く**（Zod / TS interface）。

## 2. UI 要求から逆算するデータ契約（家計分析が中心）

家計分析コンテキストの集約 / Read Model 設計時に以下の画面要求を満たす API が必要:

| 画面 | 必要データ | 集約・参照 |
|---|---|---|
| ダッシュボード KPI 4 枚 | 当月支出（世帯/個人）/ 貯蓄残高（SMBC + 別銀行）/ NISA 積立原資 / 資産合計 | 取引集約（家計分析）+ 口座集約（残高・資産推移管理） |
| ダッシュボード ドーナツチャート | 当月のカテゴリ別合計（世帯 or 個人モード） | 取引集約 |
| 月次レポート ヒーロー | 当月支出 + 前月比 + 前年同月比 | 取引集約 + 月次レポート集約 |
| 月次レポート 残高推移 | 直近 6 ヶ月の 4 軸時系列 | 月次レポート集約（残高推移パート）|
| 取引一覧 | 当月の取引（フィルタ可）+ プライバシー適用 | 取引集約 + アプリユーザー（プライバシー判定用） |
| 経費精算管理 | 当月の経費種別ごとの累計（合計のみ） | 月次経費サイクル集約 |

Phase 4 では **「画面 → データ契約 → リポジトリ I/F → 集約」** の方向で逆算してスキーマを定義する。

## 3. プライバシー 3 段階の実装位置（Phase 4 で決める）

選択肢:
- **(A) ドメインサービス層で適用**: `behavior 取引一覧を取得する(操作者ユーザーID, フィルタ)` の出力をプライバシールールでフィルタ
- **(B) Read Model 層で適用**: 表示用のクエリレスポンスを構築する段階でフィルタ（CQRS 風）
- **(C) リポジトリ層で適用**: ストレージから取り出す時点で除外

推奨: **(B) Read Model**。理由は (i) ドメインモデルは「ありのまま」を表現すべき (ii) 同じ取引データを世帯モード / 個人モードで違う見え方にする必要があるため、表示直前のレイヤで判定する方が自然。Phase 4 でこの方針を spec 化。

## 4. 状態管理 / API ライブラリ選定（Phase 4 で決める）

UI 側で必要な実装観点（Phase 3.5 で決めなかった）:
- 状態管理: TanStack Query（推奨）/ Zustand 等
- フォーム: React Hook Form + Zod resolvers
- LIFF SDK 連携層（OAuth・openWindow external）
- 取引リストの楽観的更新（編集モーダル保存時）

## 5. 残課題（OQ-31〜34）

[03-open-questions.md §B](../../domain/03-open-questions.md):
- OQ-31 アプリ名確定
- OQ-32 三井住友銀行 URL パターン
- OQ-33 LINE Flex Message サイズ
- OQ-34 テーマカラー切替

これらは Phase 4 開始前 or Phase 4 中に解決する想定。

## 6. Phase 4 ブレスト時の起点質問

writing-plans 起動前のブレスト（superpowers:brainstorming）で確認すべき問い:

1. Phase 4 のスコープは「全 8 コンテキストの戦術設計」か、それとも「家計分析 + 残高・資産推移管理（Core）の 2 つだけ先行」か
2. TS 型と Zod スキーマの記述粒度（kawasima ミニ言語からの 1 対 1 翻訳 / より TS 慣用句に寄せる）
3. リポジトリ I/F の同期 / 非同期 シグネチャ（async 必須かどうか）
4. プライバシー 3 段階の実装位置（§3 推奨）の合意確認
5. ファイル構成（packages/domain/<context>/aggregates/... のようなモノレポ前提か）

## 7. 参考: Phase 4 ファイル構成案（仮）

```
packages/
├─ domain/                      (Phase 4 でスケルトン作成)
│   ├─ shared/
│   │   ├─ value-objects/        (金額 / 発生日時 / カテゴリ等)
│   │   └─ events.ts            (ドメインイベント基底)
│   ├─ transaction-import/      (08a 取引取込)
│   ├─ classification-learning/  (08b 自動分類学習)
│   ├─ household-analysis/       (08c 家計分析)
│   ├─ balance-asset-tracking/   (08d 残高・資産推移管理)
│   ├─ expense-reimbursement/    (08e 経費精算)
│   ├─ onboarding-auth/          (08f オンボーディング・認証)
│   ├─ notification-delivery/    (08g 通知配信)
│   └─ master-management/        (08h マスタ管理)
└─ ...
```

これは Phase 4 ブレストの議論材料、確定ではない。
```

- [ ] **Step 5.2: ファイル作成を確認 + コミット**

```bash
ls docs/superpowers/plans/2026-05-01-phase4-handoff.md
git add docs/superpowers/plans/2026-05-01-phase4-handoff.md
git commit -m "docs(superpowers): Phase 4 引き継ぎサマリを追加

Phase 3.5 から Phase 4 戦術設計に渡る決定事項とブレスト起点質問を整理。
画面要求から逆算するデータ契約、プライバシー実装位置の選択肢、
状態管理ライブラリ選定の論点を含む。"
```

---

## Task 6: Phase 3.5 spec の DoD と未確定事項を更新

**Files:**
- Modify: `docs/superpowers/specs/2026-05-01-phase3.5-ux-ui-design.md`（§15 / §16）

**Goal:** §15 未確定事項のうち §B に移管したものをマーク、§16 DoD のチェックを更新。

- [ ] **Step 6.1: §15 未確定事項にステータス追記**

`docs/superpowers/specs/2026-05-01-phase3.5-ux-ui-design.md` の §15 を以下に置換:

```markdown
## 15. 未確定事項

- **アプリ名** — 候補（ふたりばこ / ふたり家計 / おうち家計 等）から後日確定。本 spec 内では無冠 or 「（仮）」表記。 → [03-open-questions.md OQ-31](../../domain/03-open-questions.md) として正式化済
- **三井住友銀行（SMBC ダイレクト）の月別明細 URL パターン** — Phase 4 実装時に確認 → [OQ-32](../../domain/03-open-questions.md)
- **明細 URL のセッション切れ時の挙動** — ブックマーク不可なら「ログイン後に該当ページを探す」手順に変更（OQ-32 に内包）
- **LINE Flex Message のサイズ制限** — リマインダーで各人別のリンク 2 個 × 2 = 4 個収まるか → [OQ-33](../../domain/03-open-questions.md)
- **テーマカラー切替** — 設定画面に placeholder のみ。将来的に複数テーマを提供するかは未定 → [OQ-34](../../domain/03-open-questions.md)
```

- [ ] **Step 6.2: §16 DoD を更新**

§16 を以下に置換:

```markdown
## 16. Definition of Done（Phase 3.5 完了条件）

| # | 条件 | ステータス |
|---|---|---|
| 1 | 本 spec が `docs/superpowers/specs/2026-05-01-phase3.5-ux-ui-design.md` にコミット済み | ✓ 完了（commit 89bde20） |
| 2 | ユーザーが本 spec をレビュー・承認済み | ✓ 完了（2026-05-01 ユーザー承認） |
| 3 | Visual Companion のワイヤーフレームが `.superpowers/brainstorm/` に保存され、参照可能 | ✓ 完了 |
| 4 | DDD docs（07/08d/08f/09/03）に Phase 3.5 変更が反映済み | ✓ 完了（commit 2cc5864） |
| 5 | デザイントークンとワイヤーフレームインデックスが `docs/domain/wireframes/` に存在 | ✓ 完了（本クローズアウト計画 Task 1〜2） |
| 6 | Phase 3.5 残課題が `03-open-questions.md §B` に OQ 番号付きで記録済み | ✓ 完了（本クローズアウト計画 Task 4） |
| 7 | Phase 4 戦術的設計の writing-plans 起動準備完了（引き継ぎサマリ作成済み） | ✓ 完了（本クローズアウト計画 Task 5） |
```

- [ ] **Step 6.3: 変更箇所を確認 + コミット**

```bash
sed -n '/^## 15. 未確定事項/,/^## 17/p' docs/superpowers/specs/2026-05-01-phase3.5-ux-ui-design.md
git add docs/superpowers/specs/2026-05-01-phase3.5-ux-ui-design.md
git commit -m "docs(superpowers): Phase 3.5 spec の §15/§16 を更新

未確定事項を OQ-31〜34 にリンクし、DoD 7 条件のチェックを反映。"
```

---

## Task 7: Phase 3.5 完了確認

**Goal:** spec §16 DoD の 7 条件をすべて満たすことを確認し、必要なら最終コミット。

- [ ] **Step 7.1: ファイル存在確認**

```bash
ls -la docs/superpowers/specs/2026-05-01-phase3.5-ux-ui-design.md \
       docs/superpowers/plans/2026-05-01-phase3.5-closeout.md \
       docs/superpowers/plans/2026-05-01-phase4-handoff.md \
       docs/domain/wireframes/design-tokens.md \
       docs/domain/wireframes/README.md
```

すべて存在することを確認。

- [ ] **Step 7.2: DoD 7 項目を再点検**

spec §16 を読み直し、すべて `✓ 完了` になっていることを確認。

- [ ] **Step 7.3: git log で本計画関連コミットを並び確認**

```bash
git log --oneline -10
```

期待するコミット（順不同）:
- `docs(domain): Phase 3.5 デザイントークンを wireframes/ に追加`
- `docs(domain): Phase 3.5 ワイヤーフレームインデックスを追加`
- `docs(domain): Phase 3.5 残課題を §B に追記 (OQ-31〜34)`
- `docs(superpowers): Phase 4 引き継ぎサマリを追加`
- `docs(superpowers): Phase 3.5 spec の §15/§16 を更新`
- 既存: `docs(superpowers): Phase 3.5 UX/UI 設計 spec を追加`
- 既存: `docs(domain): Phase 3.5 のドメイン変更を過去 DDD 資料に反映`

- [ ] **Step 7.4: 完了宣言（任意）**

特に追加変更がなければスキップ。Phase 3.5 はクローズ、Phase 4 ブレストへ移行可能。

- [ ] **Step 7.5: ユーザーに Phase 3.5 完了報告 + Phase 4 ブレスト開始の確認**

報告内容:
- 完了したタスク数: 6（design-tokens / wireframes README / 整合性検証 / 残課題追記 / 引き継ぎサマリ / spec §15-§16 更新）
- 新規ドキュメント: 4 ファイル
- 残課題: OQ-31〜34（Phase 4 で詰める）
- 次ステップ提案: superpowers:brainstorming で Phase 4 戦術的設計のブレスト開始

---

## Self-Review

### Spec coverage
- DoD #1（spec コミット）→ 既達 ✓
- DoD #2（ユーザー承認）→ 既達 ✓
- DoD #3（モック保管）→ Task 2 でインデックス化 ✓
- DoD #4（DDD 反映）→ Task 3 で整合性検証 ✓
- DoD #5（design-tokens / wireframes README）→ Task 1, 2 ✓
- DoD #6（残課題 §B 記録）→ Task 4 ✓
- DoD #7（Phase 4 引き継ぎ準備）→ Task 5 ✓

### Placeholder scan
- 「TBD」「TODO」「実装する」のような曖昧表現なし
- すべての Step に具体的なファイルパス・コマンド・コードブロックあり
- OQ 番号は Step 4.1 で「最大+1」と明示し動的に決まるが、推測値（31〜34）も記載

### Type / 命名 consistency
- ファイル名規約: `2026-05-01-phase3.5-*` で統一
- ロール表記: Honey / Darling（spec §3.1 と整合）
- ニックネーム属性: `nickname` または `ニックネーム`（kawasima 記法では後者）

問題なし。
